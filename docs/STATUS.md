# 진행 상황 — 2026-09-11 기준

> 이어서 작업할 때 **이 문서부터** 읽으면 된다. 그다음 `CLAUDE.md` → `docs/GDD.md` → `docs/HOOK.md`.

---

## 한 줄 요약

**게임은 돌아간다.** 캠페인 50판 + 무한 구간 · 화살 9종(발사음도 9색) · 활 5자루 ·
**갑옷 3벌(담금질)** · 로그라이트 여정 · 갈림길 2택 · 폭탄 과녁 · 11판+ 전면 적 전환
(창문 사수·숨는 사수·**매**·**화차**·**총통수**·**투석군**·정예·갑옷) · **보스 8종**(유령 넷은 뜨고,
도깨비·구미호·장승·저승사자는 **걷는다** — 약점 문법 넷, 그리고 **여덟이 저마다 쏜다**) ·
**패링(환도)** · 화살 맞불 · 칭호 · 샌드박스 · 성장 · 저장 · 실사운드 · **PWA** ·
**행로도(지도)** 까지 붙어 있고, 게이트(타입체크·테스트 **273건**·프로브 **17종**·빌드)가
전부 초록불이다. 재화는 **냥(兩)** 이고 어디서나 **엽전**과 함께 나온다.
**폰 세로 화면에서도 돌아간다** (2026-08-31). 켤 때는 오프닝이 **가로로 눕히라고** 그림으로 알려준다.

> 번들 — 전송 gzip **126.1KB / 150KB** · 파싱 raw **362.2KB / 400KB** (`npm run budget`, 2026-09-11).
> 상한은 2026-09-10 형의 결정으로 110/300 → 150/400 이 됐다 — **"크기는 2순위야."**
> 실제로 소리·그림·글꼴은 `public/` 이라 이 숫자에 안 들어간다. 여기서 재는 건 JS 뿐이고,
> 이 상한이 진짜로 막아야 할 것은 몰래 들어온 라이브러리 하나다 (A6).
> 글꼴(public/fonts, 45KB css + woff2 조각 95개)은 public 애셋이라 예산 밖이다 —
> 첫 페인트를 막지 않고, 브라우저는 화면에 실제로 나온 글자가 든 조각만 받는다.

### 2026-09-11 추가 38 — GAME OVER 가 주인공이다 · '몰기'를 안 쓴다 · 강화에도 그림

형: **"실패화면에는 수렵총 말고 다른걸 써야겠는데. 아예 이미지 안 쓰고 Game Over를 크게 쓰던가.
143판보다 게임오버가 더 중요하잖아. 왤케 텍스트가 쓸데없이 많아. 몰기는 이 몰기가 뭐야 대체.
대장간은 이미지 잇으니 강화도 이미지 있어야지."**

#### ① 끝난 화면 — 제일 큰 것이 하나여야 한다

죽은 직후 화면의 머리에 이런 것들이 한꺼번에 있었다:

| 있던 것 | 크기 |
|---|---|
| 수렵총 벽화 배경 + 그림 이름 | 머리의 절반 |
| "실패 — 쓰러졌다. 이번 여정은" | 14px |
| **143판** | 48px ← 제일 큼 |
| 규칙 설명 한 줄 | 13px |
| 최고 기록 한 줄 | 13px |
| 가져간다 한 줄 | 14px |
| 다음 여정 한 줄 | 13px |

일곱 줄이 그림 위에 얹혀 있고, 그중 **제일 큰 것이 판 수**였다. 그런데 형이 그 화면에서
제일 먼저 알아야 하는 건 "끝났다"지 "143"이 아니다. 숫자가 제일 크면 화면이 성적표가 되고,
성적표는 죽은 직후에 읽고 싶은 것이 아니다.

- **그림을 뺐다.** 끝난 화면에 볼거리를 얹는 건 위로가 아니라 소음이다.
- **GAME OVER 를 52px 로** 세웠다 (판 수는 26px). 접었을 때는 같은 자리·같은 크기에
  '여정 종료'가 회색으로 뜬다 — 접은 것은 실패가 아니다.
- **줄을 일곱에서 다섯으로.** 규칙 두 줄을 '왜 끝났는가' 한 줄로 합치고, 판 수와 기록을
  한 줄로 붙였다 (둘 다 같은 것을 재는 숫자다).

#### ② "몰기는 이 몰기가 뭐야 대체"

연속 5중이면 "가져간다" 줄에 **몰기**라고만 떴다. 국궁의 말이고, HUD 눈금을 지운 뒤로는
그 말을 배울 자리가 화면 어디에도 없다. 낱말을 가르치느니 뜻을 준다 — 이제 **연달아 5발**이라고
쓴다. 메커닉(`World.molgi`)은 그대로다. 화면에 쓰는 말만 바꿨다.

#### ③ 강화에도 머리 그림

대장간에는 김홍도 「대장간」이 걸려 있는데 바로 옆 칸인 능력치에는 아무것도 없었다.
한 화면에서 한쪽만 그림이 있으면 **그림 없는 쪽은 덜 만든 칸으로 읽힌다.**
끝난 화면에서 내려온 수렵총 벽화(호랑이를 쫓는 기마 궁수)를 여기 걸었다 — 몸을 키우는 칸의
그림으로 이만한 게 없다. 대장간 머리와 **같은 뼈대**(`.s-h` ↔ `.f-h`)라 한 벌로 읽힌다.
성장 화면의 능력치 칸과 끝난 화면의 강화 칸, 두 군데 다 선다.

#### 프로브

`tools/probe-layout.ts` 9번을 새로 넣었다. 다시 늘어나기 쉬운 것들만 못 박는다:
머리에 `url(` 이 없는가 · GAME OVER 의 font-size 가 판 수보다 큰가 · 머리의 `<div>` 줄이
다섯 이하인가 · 실제로 만든 HTML 에 '몰기'가 없는가 · 두 화면 다 `.s-h` 가 서는가.
전부 통과 (325개).

---

### 2026-09-11 추가 37 — 살통이 위아래로 돈다 · 컴파운드가 작던 이유 · 유엽전은 화살이다

#### ① "자연스럽게 위아래로 리볼빙 되어야 하는데 양옆으로 넘겨야 하고 넘기면 대각선으로 흔들려"

세 가지가 겹쳐 있었다.

1. **축이 틀렸다.** 버튼 줄은 이미 **좌우**가 다른 버튼들의 축이다. 거기서 또 좌우로 밀면
   서로 싸운다. `makeWheel` 에 `axis: 'y'` 를 더해 살통은 **위아래**로 돈다 —
   리볼버의 실린더가 도는 그 방향이고, 한 번에 한 칸만 보이므로 자리도 안 먹는다.
   화살표도 ⌃ ⌄ 로 바뀌어 오른쪽에 세로로 쌓인다.
2. **통째로 움직였다.** 고리가 넘어가는 칸이 +2 에서 −2 로 **화면을 가로질러 미끄러졌다.**
   이제 보이는 범위 밖의 칸은 transition 을 꺼서 **순간이동**한다.
3. **대각선으로 떨었다.** `layout()` 이 매번 `style.width` 를 다시 써서
   `translate(-50%)` 의 기준점이 흔들렸다. 값이 바뀔 때만 쓴다.

#### ② "살통걸이가 모든 화면에서 보이고"

살통은 **쏘는 중에** 바꿔 드는 물건인데 오프닝·출정·성장·지도 위에도 비쳤다.
판이 열려 있으면(`.hb-scrim.hb-open`) 숨는다.

#### ③ "컴파운드보우는 이미지가 혼자 너무작아. 이거 텍스트 때문 같은데"

**정확한 진단이었다.** 그림이 `flex: 1` 이라 남는 높이를 먹는데, 컴파운드의 설명만
27자("도르래 덕에 끝까지 당긴 채 오래 버텨도 힘이 덜 빠진다")라 글자가 두 줄을 더 먹고
그만큼 그림이 눌렸다. 그림 다섯 장의 크기는 **원래 같았다** (255~268 × 320).
이제 그림의 몫을 **고정**하고(카드의 58%), 설명은 **두 줄에서 자른다** — 나머지는 길게 누르면 뜬다.
실측: 카드 230px 중 그림 125px (54%), 설명 줄 17px. 글자 길이와 무관해졌다.

#### ④ "기본화살은 곱하기 무한대 표시 (…) 진짜 무슨 화살표처럼 만들어버리니까"

- 유엽전 칸에 **×∞**. 빈칸이면 "안 적은 것"인지 "0발"인지 모른다.
- 아이콘을 다시 그렸다. 옛것은 〈가로줄 + 꺾쇠〉 — 그건 "다음으로"를 뜻하는 **UI 글리프**지
  물건이 아니다. 화살을 화살로 만드는 건 양 끝이다: **버들잎(柳葉) 촉**(닫힌 잎)과
  **깃 둘 + 오늬 홈**. 이 셋이 붙으면 방향 표시와 안 헷갈린다.

---

### 2026-09-11 추가 36 — 활 그림 다시 · 가로 폰의 좁은 칸 · 화약궤 · 파비콘 캐시

#### ① "활 이미지가 이상하게 잘렸는데? 활줄이랑 활대 사이가 (…) 어색하게 남아있잖아"

맞다. 첫 판은 **손이 셋이나 갔다** — 세로로 선 활을 받아 ⑴ 잉크를 세어 덩이를 찾고
⑵ 바탕을 알파로 따내고 ⑶ 카드 안에서 −28° 로 돌렸다. **활줄과 활대 사이**는 바탕도 물건도
아니라서, 따내면 구멍이 되고 남기면 네모가 됐다. 손이 갈수록 어긋난다.

이제 **그리는 쪽에서 끝낸다.** 칸마다 한 자루씩 **대각선으로 꽉 차게** 그린 1×5 띠를 받는다 —
줄과 활대 사이는 금빛 후광이 채워 주고, 그게 그림의 일부다. 여기서 할 일은 **다섯으로
나누고 빈 여백만 떼는 것**뿐이다. 따낼 것도 돌릴 것도 없다.
비율은 **안 늘린다** (정사각으로 맞추면 활이 눌린다) — 다섯이 266×320 언저리로 한 벌이다.

#### ② "가로로 눕혀서 봤을때 (…) 스크롤가능칸이 엄청좁아"

재 보면 그렇다. 세로 414px 폰에서 주소창을 빼면 **330px**인데, 그 위에
스크림 20 + 판 24 + 몸통 14 + 머리 그림 84 + 탭 42 + 아래 줄 50 이 먼저 먹는다 →
**내용에 남는 것이 100px 남짓.** 낮은 화면(≤560px)에서 전부 조였다:

| 무엇 | 전 | 후 |
|---|---|---|
| 스크림 여백 | 10 | 6 |
| 판 여백 · 문양 테두리 | 12 · 16 | 8 · 10 |
| 탭 높이 | 42 | 34 |
| 아래 줄 버튼 | 44 | 38 |
| 출정 머리 그림 | 84 | **한 줄로 눕힌다** (안내문·그림 이름 감춤) |
| 걸이 | 158 | min(148, 44vh) |
| 카드 최소폭 | 132 | 112 (한 줄에 더 들어간다) |

#### ③ "폭탄박스 전혀 화약상자처럼 안보이니까 수정해"

예전 것은 **활색 네모에 ×자 두 줄**이었다 — 포장 상자다. 화약궤로 만드는 넷을 넣었다:
**두께**(뒤로 물러난 윗면 — 이 한 면이 상자를 만든다) · **쇠테 둘과 징 넷** ·
**붉은 인장에 火** (작으면 인장만) · **도화선과 불씨**. 색도 활색에서 어두운 나무로 바꿨다.

#### ④ "앱 켰을때 화면에 무슨 화살표 박스 하나가 화면 좌하단에 고정돼있어"

살통 걸이의 화살표(‹ ›)였다. 폰 규칙 `.hb-hud .hb-btn { min-width:46px; height:46px }` 이
**걸이의 화살표까지 잡아서** 46px 짜리 네모가 됐다 — 데스크탑에서는 안 보이는 버그다.
`.hb-hud .wh-arm` 으로 되돌렸다.

#### ⑤ "파비콘 아직도 안된거같은데"

커밋·푸시·배포는 되어 있었다 (`f4d96f1`, 배포본과 로컬 파비콘이 **바이트 단위로 같다**).
문제는 **크롬이 파비콘을 일반 캐시와 다른 곳에 따로 저장**하고 그 자리는 강제 새로고침으로도
잘 안 비워진다는 것이다. → 링크에 `?v=2` 를 붙여 **주소를 바꿨다.** 그림을 다시 구울 때마다
이 숫자를 올린다 (`index.html` 주석).

---

### 2026-09-11 추가 35 — 엽전만 · 활은 그림으로 · 살통도 걸이로 · 파비콘

#### ① "냥이란 단어 쓰지 말고 엽전과 숫자만 해"

맞다. 엽전 그림이 이미 "이건 돈이다"를 말하는데 그 옆의 글자는 군더더기였다.
`coinText()` 는 이제 **숫자만** 돌려준다. 값이 뜨는 자리(부적·갑옷·살 가게·오프라인 안내)는
`coinHtml()` 로 바꿔 **엽전 그림이 숫자 앞에 선다.**

#### ② "활도 각각 이미지로 등록되어야해. 개떡같은 svg 말고. 선택할때만큼은 화려해야지"

챗지피티에서 **한 장에 다섯 자루**를 받아(`assets_src/bows-source.png`) `tools/slice-bows.mjs`
로 잘랐다. 다섯을 따로 받으면 그림자 각도와 붓질이 제각각이 되는데, 한 붓에서 나오면
걸이에서 **한 벌로** 읽힌다. 자를 자리는 눈대중이 아니라 **세로줄마다 잉크를 세어** 찾는다.

- **바탕은 투명하게 딴다.** 바탕과의 거리로 알파를 만들어 금빛 후광은 반투명으로 남긴다.
- **카드 안에서 비스듬히 세운다** (−28°, 1.55배 / 앞에 나온 것은 −24°, 1.68배).
  활은 세로로 8:1 이라 똑바로 세우면 카드 안에서 실오라기가 된다. 대각선이 네모를 제일 크게 쓴다.
- 잠긴 활은 그림을 안 준다 — 실루엣까지 보이면 가려진 게 아니다.

#### ③ "화살 아직도 게임화면에서 하나하나 버튼인데 이거 리볼빙되듯"

살 하나에 버튼 하나였다. **자리가 종류 수에 비례하는 UI 는 언젠가 화면을 먹는다** —
실제로 그 줄이 접히면서 궁수를 덮었다. 이제 `ui/wheel.ts` 에 **슬림** 모드를 더해
살통도 걸이 하나가 됐다 (버튼 높이 52px, 카드가 한 줄로 눕는다).

- **유엽전이 걸이의 첫 칸이다.** 특수살을 내려놓는 것이 이제 **돌리는 동작 하나**다
  (예전엔 같은 버튼을 두 번 누르는 숨은 규칙이었다).
- 다 쓴 살에도 멈출 수 있다 — 0발이 보여야 아쉽다. 들리지는 않고 왜인지 한 줄이 말한다.
- 종류가 아홉이 되어도 **넓이가 안 변한다.**

#### ④ "아주작은 신궁 아이콘이 아직도 활과 화살모양이야"

두 가지가 겹쳐 있었다.
1. **`<link rel=icon>` 이 192px 한 장뿐**이라 브라우저가 16px 로 줄이느라 뭉갰다.
   → `favicon-32.png`·`favicon-64.png` 를 미리 굽고 `sizes` 를 적어 준다.
2. ★ **서비스워커가 옛 아이콘을 붙들고 있었다.** `public/` 파일은 이름이 안 바뀌는데
   sw 가 **캐시 우선**이라, 새로 배포해도 이미 연 사람에게는 옛 그림이 계속 나온다.
   → `CACHE` 를 `hanbal-v1` → `v2` 로 올렸다 (activate 가 옛 캐시를 지운다).
   **public/ 의 그림·소리를 갈아 끼울 때마다 여기를 올려야 한다.**
3. 덤으로 **maskable** 도 고쳤다. 안드로이드는 maskable 아이콘을 원으로 깎는데 초상화가
   가장자리까지 차 있어 활과 머리가 잘렸다 → `icon-maskable-512.png` 를 따로 굽는다
   (그림을 78% 로 줄여 테마색 위에 가운데).

프로브 `probe-layout` 에 두 절을 더했다 — 살통이 걸이 하나인가(옛 `.q-btn` 이 0개인가),
활 그림 다섯이 다 있고 투명 배경인가.

---

### 2026-09-11 추가 34 — ★ 브라우저가 된다 · 아이콘을 챗지피티에서 받아왔다

형: **"브라우저에서 챗지피티 열면 되잖아. 너 여태까지 그러면 이미지 뭘로어떻게 만든건데"**

두 번째 물음부터 정직하게: **그림 생성 도구는 한 번도 안 썼다.** 이 저장소의 그림은 셋뿐이었다 —
① 코드로 캔버스에 찍은 것(적·보스·지도) ② PNG 를 zlib 으로 **바이트째 인코딩**한 것(아이콘)
③ 받아온 것(Kenney 테두리·game-icons·김홍도). 그래서 그 모양이었던 것이다.

#### ★ 그리고 CLAUDE.md 의 "브라우저 도구 금지"는 **틀린 전제였다**

그동안 "연결된 크롬은 다른 기기라 localhost 가 영원히 안 열린다"고 적혀 있었다. 직접 해 보니:

| 해 본 것 | 결과 |
|---|---|
| `switch_browser` → 형의 크롬("desktop") | 붙는다 |
| `npx vite --port 5173` → 크롬에서 열기 | **200. 화면이 다 보인다** |
| 스크린샷 · 콘솔 · `javascript_tool` | 전부 동작 |
| 크롬 다운로드 → `~/Downloads` | **이 데스크탑에 떨어진다** (같은 기기다) |

**함정 하나**: 자동화가 잡는 탭은 `document.hidden` 이라 **rAF 가 안 돈다 → 캔버스가 새까맣다.**
그건 버그가 아니라 규칙 4(백그라운드에서 안 돈다)가 제대로 도는 것이다. DOM 패널은 hidden 이어도
보이므로 **UI 작업은 브라우저로 확인이 된다.** CLAUDE.md 를 이 내용으로 통째로 고쳐 썼다.

#### 열어 보자마자 찾은 것들 (프로브로는 못 찾았을 것들)
- **판이 760px 인데 안쪽이 317px 만 쓰고 있었다** — `.hb-body` 에 `flex: 1` 이 없어 내용
  높이로 줄어들고, 남은 440px 이 통째로 빈 종이였다. 어제 "빈 공간이 많다"의 진짜 원인.
- **말풍선이 자기 카드를 덮고 있었다** — 크기를 `getBoundingClientRect` 로 재는데 내용을 막
  넣은 참이라 높이가 거의 0으로 나왔다. `offsetHeight` 로 바꾸고, 위아래가 좁으면 **옆**으로 뺀다.
- **대장간 칸에 가로 스크롤바** — 머리 그림의 음수 여백(-8px)이 만든 것. `overflow-x: hidden`.
- **엽전이 점처럼 보였다** — 13px 글자 기준이라 숫자(24px) 옆에서 먼지였다. 22px 로.
- **걸이가 큰 화면에서 아래를 못 채웠다** — 무대 높이를 `clamp(168px, 38vh, 280px)` 로,
  카드가 무대의 82% 를 차지하게. 활 걸이 칸은 내용을 가운데로 모은다(`.hb-mid`).

#### 아이콘 — 챗지피티에서 받아 굽었다
형의 브라우저에서 챗지피티(로그인돼 있다)에 `docs/ICON.md` 의 프롬프트를 그대로 넣어 그림을
받고, 다운로드 → `assets_src/icon-source.png` (1254×1254) → `node tools/bake-icon.mjs` 로
512·192·180 을 굽었다. **비장한 다윗** 그대로다: 시위를 안 당긴 채 활을 세워 쥐고 선 궁수,
뒤에서 오는 금빛 역광, 다른 손에 화살 한 대. 192px 로 줄여도 읽힌다.
`probe-icon` 은 받아온 그림이면 색으로 재는 검사를 건너뛴다 (방향은 눈으로 본다).

---

### 2026-09-11 추가 33 — 아이콘의 활이 거꾸로였다

형: **"신궁아이콘 ㅋㅋㅋ 병신아 활을 꺼꾸로 쏘냐???"**

맞았다. 숫자로 재 보면 활채가 가장 부푼 곳 **u = −0.220**, 시위 **u = −0.035**, 화살은
오른쪽(촉 u = +0.58). 화살이 오른쪽으로 나는데 활채가 시위보다 **왼쪽**이면 그건 활을
뒤집어 쥔 그림이다. 오늬(−0.245)도 활채보다 뒤라 **화살이 활을 뚫고** 있었다.

- 고쳤다 (`tools/make-icons.mjs`). 규칙 하나에서 전부 나오게 다시 썼다:
  **화살은 촉 쪽으로 난다 · 시위는 오늬 자리 · 활채의 배는 그 앞.**
  손잡이를 짙게 넣어 쥐는 자리를 만들고, 뒤로 옅은 금빛과 네 귀의 어둠을 깔았다.
- **`tools/probe-icon.mjs` (프로브 17종)** — 구워진 PNG 를 **도로 뜯어서** 잰다.
  코드가 아니라 그림을 읽는다. 거꾸로 그린 판을 일부러 넣어 보니 빨간불이 떴다.
  > 처음 판은 **헛통과**했다. 활채의 가장 오른쪽 점을 배로 쳤는데 **깃이 같은 색**이라
  > 깃을 배로 착각했다. 손잡이 색(오직 배에만 쓴다)으로 재도록 고쳤다.
  > **색이 겹치면 재는 것도 겹친다.**
- **이 그림은 임시다.** 형이 말한 "비장한 다윗" 은 사람이 나와야 하고 그건 손으로 그릴
  그림이 아니다. `docs/ICON.md` 에 챗지피티에 **그대로 붙여넣을 프롬프트**(영어·한국어),
  받은 그림이 틀렸는지 보는 법 넷, 그리고 굽는 법을 적어 뒀다.
- **`tools/bake-icon.mjs`** — 받아온 PNG 한 장 → 512·192·180 세 장. 한 줄이다.
  정사각형이 아니면 가운데를 자르고, 투명한 자리는 테마색으로 받고, 상자 평균으로 줄인다
  (크게 줄일 때 점이 안 튄다). 필터 다섯을 다 푸는 PNG 디코더를 붙였다 — 라이브러리는 안 쓴다 (A6).
  갈아 끼우면 `probe-icon` 은 색으로 재는 부분을 건너뛴다 (그림의 방향은 형이 눈으로 본다).

---

### 2026-09-11 추가 32 — 문서가 아니라 게임 화면 (탭 · 걸이 · 설치 버튼)

형의 반려 셋을 한 묶음으로.

#### ① "활선택하는게 버튼이 아니라 롤이었으면 좋겠어. 리볼빙형식이라고 해야하나?"

`src/ui/wheel.ts` — **돌아가는 걸이**. 한 자루가 앞에 나오고 나머지는 옆으로 물러난다.
화살표·손가락 밀기·좌우 키 셋 다 통하고, **고리라 끝에서 한 번 더 돌리면 처음으로** 간다.
자리 계산은 고리 위의 최단 거리라(5자루에서 4→0은 +1) 도는 방향이 누른 화살표와 같다.
잠긴 활도 걸어 둔다 — **보여야 갖고 싶어진다.** 거기 멈춰도 드는 활은 안 바뀌고,
아래 한 줄이 "아직 못 든다 · 지금 드는 활은 ○○" 라고 분명히 적는다.
출정 화면과 성장의 활 걸이가 **같은 부품**을 쓴다.

#### ② "능력치강화랑 대장간 이런거 한 화면에 세로로 몰아넣는게 맞아?" · "세로롤링 너무 게임 안같고 뭔 웹페이지 같잖아"

둘 다 같은 말이고 맞는 말이었다. 판이 **긴 문서**였다 — 능력치 아래 활 걸이, 그 아래 대장간,
그 아래 설정. 두 단으로 갈라 봤지만(추가 30) 그건 문서를 **두 줄로** 만든 것뿐이다.

`src/ui/tabs.ts` — **판의 높이는 화면이 정하고**(`.hb-tall`), 내용은 위의 칸으로 갈아탄다.

| 판 | 칸 |
|---|---|
| 성장 | 능력치 · 대장간 · 활 걸이 |
| 출정 | 활 · 갑옷 · 부적 · 살 가게 |

**아래 줄은 늘 보인다** — '나선다'가 굴려야 나오면 그건 게임이 아니다. 지금 무엇을 들고
나가는지도 거기 한 줄로 적힌다(활 · 갑옷 · 부적), 어느 칸을 보고 있든.
한 칸에 쌓이는 줄: 성장 **8 → 4** · 출정 **11 → 6**.

#### ③ "왜 아직도 앱으로는 다운 안받아지는거야?"

**서버 쪽은 처음부터 멀쩡했다.** manifest(`application/manifest+json`)·`sw.js`·아이콘
192/512 전부 200 이고 규격도 맞는다(실측: 192×192, 512×512). 문제는 **화면에 누를 것이
없었다**는 것이다 — 2026-09-10 에 "설치 안내는 하지 않는다(GDD 9장)"고 적었는데, 그 조항이
막으려던 건 광고·출석보상 같은 **조르기**지 "설치하는 법"이 아니었다. 브라우저 메뉴를 아는
사람만 설치할 수 있는 건 기능이 없는 것과 같다.

`src/ui/install.ts` — **브라우저가 된다고 할 때만**(`beforeinstallprompt`) 뜨는 작은 단추.
출정·성장 화면의 아래 줄 한 곳뿐이고, 게임 중에는 안 뜬다(C1). 설치했거나 이미 앱으로
열었으면(`display-mode: standalone`) 아예 없다. 아이폰은 `beforeinstallprompt` 가 없으므로
거짓 버튼 대신 **방법을 한 줄로** 띄운다: "사파리 공유 단추 → 홈 화면에 추가".
신호는 로드 직후 한 번만 오므로 `main.ts` 가 **시작하자마자** 잡는다.

**이번에 배운 것**
- **클래스 이름이 부분문자열로 겹치면 프로브가 딴 걸 집는다.** `l-going`(요약 줄)이
  `l-go`(출정 버튼)를 품어서, 부팅 프로브가 버튼 대신 그 줄을 눌렀다 → `l-carry` 로 고쳤다.
- **탭은 처음 보일 때 onShow 를 부르면 안 된다.** 세우는 함수가 아직 안 끝나 걸이가 없다.

---

### 2026-09-11 추가 31 — 카드가 아는 것을 다 말하지 않는다 (하스스톤의 세 층)

형: **"갑옷만 봐도 지금 갑옷하나만 고르는거 봐도 화면에 꽉차는데 이거 맞냐고? 하스스톤은
작은 화면을 커버하기 위해 화면에 가장중요한 설명, 탭할때 바로뜨는 부가 설명과 (…) 조금더
오래누르고 있으면 뜨는 추가설명, 이런식으로 커버하는데 우리도 그런것좀 해야해."**

맞는 지적이다. 카드 한 장이 **아는 것을 전부** 적고 있었다 — 이름·한자·장점·대가·수치·값까지.
갑옷 카드 하나가 네 줄이고, 셋이면 화면이 찬다. 정보를 지우는 게 답이 아니라 **층을 나누는**
것이 답이다. 하스스톤이 하는 그대로 셋이면 된다.

| 층 | 무엇 | 어디 |
|---|---|---|
| ① 겉면 | 이름 + **숫자 하나** | 카드 |
| ② 누르면 | 고른다 · 그 아래 한 줄에 부가 설명이 **바로** 뜬다 | `.l-syn` |
| ③ 길게 누르면 | 나머지 전부 — 유래·대가·궁합·수치·잠금 조건 | `ui/detail.ts` 말풍선 |

- **`src/ui/detail.ts`** — 길게 누르기(400ms) · 마우스는 올려두기(480ms) · 오른쪽 버튼.
  말풍선은 **한 장만** 만들어 돌려 쓰고, 클릭을 안 먹고(pointer-events:none), 화면 밖으로
  안 나간다(위가 좁으면 아래로 뒤집는다). ★ **길게 눌러 연 그 한 번의 click 은 삼킨다** —
  자세히 보려다 장비가 바뀌면 그건 배신이다 (capture 단계에서 막는다).
- **겉면 다이어트** — 갑옷은 〈이름 · **막는 양 30** · 입고 나선다〉 셋뿐. 한자·설명·상한·
  판에서 사는 값은 말풍선으로. 활 카드는 대가·궁합이 빠지고, 성장의 활 걸이 줄도 같다.
  카드 겉면 **4줄 → 3줄**, 그리드도 150px → 132px 로 촘촘해져 한 줄에 더 들어간다.
- **안내 한 줄** — 구역 머리 끝에 아주 작게. 손가락 화면은 "길게 눌러 자세히",
  마우스 화면은 "올려두면 자세히" (`.hb-tip-touch` / `.hb-tip-mouse`).
- **② 층을 새로 깔았다** — 활에만 있던 '고른 것의 한 줄'을 갑옷에도 붙였다.
  활은 대가·궁합, 갑옷은 한 벌·상한·판에서 사는 값.

`tools/probe-layout.ts` 가 이것도 센다: 카드 12장 전부에 말풍선이 붙었고(contextmenu 로 판정),
겉면이 세 줄을 안 넘고, 부가 설명 줄이 두 곳에 있다.

---

### 2026-09-11 추가 30 — 판이 화면을 쓴다 (형: "빈공간이 너무 많아서 한눈에 안들어와")

형: **"화면이 스크롤 너무 내려야 하는데 이거. 게임화면은 넓게 쓰는데 UI 인터페이스가
너무 빈공간이 많아서 한눈에 안들어와."**

원인은 하나였다. `.hb-panel` 이 **`width: min(680px, 100%)` 에 못 박혀 있었다.**
1920 짜리 화면에서도 판은 680px 이고 나머지 1240px 은 어두운 막이다. 그러니 내용이
**세로로만** 자라고, 성장 화면은 줄 열셋이 한 줄로 쌓여 끝까지 내려야 했다.

- **폭이 화면을 따라간다** — `--pw` 680 → 880 → 1100 → 1280px (900·1200·1600px 문턱).
- **넓어진 만큼을 두 단으로 쓴다** (`.hb-cols`). 900px 밑에서는 한 단이라 **폰은 그대로다.**
  - 성장 — 왼쪽 **돈을 쓰는 것**(스탯·대장간, 같은 지갑이라 나란해야 저울질이 된다) ·
    오른쪽 **고르는 것**(활 걸이). 세로 줄 **13 → 8** (38% 짧아짐, 두 단 치우침 23%)
  - 출정 — 왼쪽 **들고 입는 것**(활·갑옷) · 오른쪽 **이번 여정에 사는 것**(부적·살 가게).
    세로 칸 **12 → 8**
  - 수집 — 왼쪽 **모은 것**(칭호) · 오른쪽 **판의 별**(격자)
- **행로도가 판을 채운다** — 종이(544px 고정)를 줄이기만 하던 것을 **키우기도** 한다 (상한 ×1.7).
  넓은 판에서 좌우가 통째로 빈 막이던 것이 이거였다.
- **세로 리듬을 조였다** — 스탯 줄 16/15 → 11/10 · 활 줄 12/11 → 9/8 · 구역 머리 24 → 16 ·
  스크림 여백 28 → 20. 줄 하나가 네 줄짜리인데 여백까지 크면 넷이 화면을 다 먹는다.
- **글줄에는 상한을 뒀다** (`.hb-lead { max-width: 64ch }`) — 넓다고 한 줄이 화면을
  가로지르면 그건 읽기가 더 나빠진 것이다.

게이트에 `tools/probe-layout.ts` 를 더했다 (**프로브 16종**). 브라우저가 없으니 픽셀은 못 재지만,
DOM 스텁으로 진짜 패널을 세워 **줄이 어느 단에 들어갔는지**를 트리에서 직접 읽는다 —
"한 단일 때 = 줄의 합 · 두 단일 때 = 긴 쪽" 이 스크롤 길이의 전부이기 때문이다.

---

### 2026-09-11 추가 29 — 형의 반려 열셋 (그림·돈·보스·새 적)

하루치를 한 묶음으로. **둘은 내가 만든 버그였다.**

#### ① 노란 동그라미 — 매·화차의 급소 표시 (형: "왜 자꾸 있는거 매나 화차나 이런거에")
뿌리는 **급소 자리가 사람 기준 하나뿐**이었다는 것이다. sim 은 "몸 중심에서 위로 0.62r" 을
급소로 봤는데, 매의 머리는 몸 **앞**에 있고 화차의 급소는 사람 머리가 아니라 **화약궤**다.
그림이 판정을 못 따라가니 렌더가 그 자리에 노란 공을 얹어 "여기요" 하고 있었던 것이다.
이제 `sim/target.ts foeWeakSpot` 한 곳이 종마다 자리를 정하고, **렌더는 그 자리에 있는 것을
그린다** — 매는 홍채·눈동자·눈빛 점 세 겹의 **진짜 눈**, 화차는 쇠테 두른 **화약궤**다.

#### ② 화차의 포수가 사람이 아니었다 (형: "화차 기수는 사람처럼 안보이고")
대각선 획 하나 + 동그라미 하나였다. 그건 사람이 아니라 깃대다. 이제 **두 다리로 땅을 딛고**,
몸통이 서 있고, 두 팔이 **화승을 발사틀에 대고**, 머리에 **전립(氈笠)** 을 썼다.

#### ③ 화차가 하늘에 떠 있었다 (형: "화차는 대체 왜 공중을 쳐 날라다니고 있는거냐?")
자리를 "전환 대상 아무거나"에서 뽑았다. 공중 과녁(aerial) 자리가 걸리면 그 높이를 그대로
물려받아 **수레가 하늘에 떴다.** 이제 ① 뽑기에서 공중 자리를 빼고 ② y 를 바닥에 못 박는다.

#### ④ 보스마다 왕눈알이 하나씩 더 박혀 있었다 (형: "도깨비새끼는 왕눈이 뇌속에")
급소가 **몸과 무관한 한 벌**이라 scene.ts 가 여덟 전부에 같은 흰 눈알을 얹었다. 유령 넷에게는
그게 몸 자체라 맞지만, 얼굴이 **이미 있는** 넷에게는 머리통 안에 눈이 하나 더 생긴 것이다.
이제 **급소는 그 몸이 원래 가진 눈이다** (`render/bosses.ts drawNewBossFace`):
도깨비는 부릅뜬 두 눈이 감겼다 뜨고 · 구미호는 세로 눈동자의 여우 눈 · 장승은 나무에 파인
왕방울 눈이 두들겨야 열리고 · 저승사자는 갓 그늘 밑의 푸른 실눈이다.
도깨비의 급소 자리도 이마에서 **얼굴**로 내렸다 (`bossWeakSpot`).

#### ⑤ 보스가 걸어오기만 했다 (형: "보스가 자기들만의 공격이 있어야 하는데 그런것도 없고")
여덟이 저마다 던진다 (`sim/target.ts bossAttack`). 판정은 잡몹이 쏘는 것과 **완전히 같다** —
방패가 막고 환도가 쳐낸다. 규칙이 하나여야 배우는 것도 하나다.

| 보스 | 던지는 것 | 발수 |
|---|---|---|
| 눈알귀신 | 안광 — **혼불(魂火)** | 1 |
| 갑주귀신 | 철편 (쇳조각) | 1 |
| 쌍눈귀신 | 혼불 (몸이 둘이라 결국 둘) | 1 |
| 폭주귀신 | 불티 — 넓게 셋 | 3 |
| 도깨비 | 방망이로 땅을 쳐 튄 **돌** | 2 |
| 구미호 | 여우불(狐火) | 3 |
| 장승 | 제 몸에서 뽑은 **말뚝** | 2 |
| 저승사자 | 적패지 — 가장 **자주** | 1 |

**비틀거리는 동안은 못 쏜다.** 이제 "눈을 노려라"가 시간 벌이가 아니라 **맞지 않는 법**이다.
밸런스: 보스 판 클리어율은 공격을 끄고 켜도 **똑같다** (1-10 85% · 4-10 20% · 나머지 0%).
그 벽은 원래부터 화살 예산의 벽이지 피해의 벽이 아니었다 (BALANCE §9-3). 죽는 비율만 늘었다.

#### ⑥ 새 잡몹 둘 — 서로 **반대 질문** (형: "항상 새로움을 줄 수 있도록 다양한 적군들")
- **총통수(銃筒手)** 21판+ — 승자총통. 어깨에 얹은 굵은 통이 서명이고, 예고 동안 **화승의
  불씨가 총구로 기어간다.** 탄환이 곧고 빠르다 → **산 방패가 잘 듣는다.**
- **투석군(投石軍)** 31판+ — 무릿매. 머리 위에서 도는 돌이 서명이고, **넘겨 던진다**
  (같은 포물선 식의 높은 해). → **방패 위로 넘어온다.** 답은 환도이거나 먼저 눕히는 것이다.

방패 하나로 다 되면 방패를 사는 것이 결정이 아니게 된다. 그래서 넘어오는 놈이 필요했다.
둘 다 실험장 목록에 있다 (**목록이 곧 존재다**).

#### ⑦ 화공이 형의 화전 재고를 녹이고 있었다 (형: "전엔 안그랬는데 누가 이따위로")
**내가 만든 버그다.** 화공은 그 판에서만 공짜로 화전을 물리는데, 물리는 방법이
`save.runArrow = 'burst'` 였다 — **세이브를 영구히 고쳤다.** 판이 끝나면 공짜 플래그만
사라지고 든 살은 화전인 채라, 다음 판부터 살 가게에서 비싸게 채운 화전이 조용히 빠져나갔다.
이제 바꿔 들기 전의 살을 적어 두고(`arrowBeforeFork`) 판이 끝나면 **그대로 되돌린다.**

#### ⑧ 재화가 돈 같지 않았다 (형: "'훈련'으로 하니까 전혀 돈버는 기분이 아니야")
'훈련치'는 가계부의 말이 아니라 체육관의 말이다. 이제 **냥(兩)** 이고, 아이콘은 **엽전(葉錢)** —
둥근 테에 네모 구멍(상평통보). 새 낱말이 아니라 이미 이 세계에 있던 말이다(육량전 = 여섯 냥).
`src/game/money.ts` 한 곳이 이름·아이콘·글자를 전부 쥔다 — 열두 파일에 흩어져 있던 '훈련치'가
언젠가 한쪽만 옛말로 남는 것을 막는다. 세이브 필드(`training`)는 **안 바꿨다** (마이그레이션 무의미).

#### ⑨ 좌상단의 '연사'·'몰기' (형: "뭔소린지도 모르겠는데 왜있는거냐고 지워버려")
눈금 다섯과 낱말을 화면에서 내렸다. **기능은 그대로다** — 연달아 맞히면 활이 가벼워지는 건
손이 느끼는 것이고, 설명이 필요한 순간 그건 이미 손맛이 아니라 숙제다. 안내 토스트도 없앴다.

#### ⑩ 패링 아이콘이 스틸레토였다 (형: "환도 모양으로 만들어")
곧은 삼각날 + 십자 가드는 서양 단검의 문법이다. 환도의 표식 셋으로 다시 그렸다:
**휜 외날 · 작고 둥근 코등이 · 자루끝의 띠돈 고리**(이름의 環이 그것이다).
화면 속 칼도 곧은 막대에서 **휜 날**이 됐고 코등이 원반이 붙었다.

#### ⑪ '숨' → '숨참기'. #### ⑫ PWA 는 **된다** (아래 참고)

#### ⑬ 에셋 — `docs/ASSETS.md` 를 새로 썼다
형: "적절한거 다운받아오던지 내가 찾아올수있는 무료 사이트 여러개 뽑아와."
여는 것까지 확인한 목록 일곱(Kenney·OpenGameArt CC0 필터·game-icons·itch.io·**공유마당**·
**문화포털 전통문양**·CraftPix)과 검색어·라이선스 표·넣는 법을 적었다. 배선(`sprite()` + 폴백)은
이미 있다. **고르는 것은 눈으로 하는 일이라** 형이 링크를 던져 주면 그때 갈아 끼운다.

> **PWA 확인 (2026-09-11)** — `https://retyper.github.io/hanbal/` 에서 manifest(200,
> `application/manifest+json`)·`sw.js`(200)·아이콘 192/512(200)가 전부 정상이다. 설치는 된다.
> 다만 **설치 안내를 화면에 띄우지 않는다** (GDD 9장). 안드로이드 크롬은 ⋮ → '앱 설치',
> 아이폰 사파리는 공유 → '홈 화면에 추가'. **아이폰은 가로 고정이 안 된다** — iOS 는
> manifest 의 `orientation`·`display:fullscreen` 을 무시하고 standalone 까지만 준다.

**이번에 배운 것**
- **`node -e` 로 파일을 고치지 않는다.** 짧은 명령 하나가 `writeFileSync(경로, '/tmp/x')` 로
  `src/sim/target.ts` 를 통째로 6바이트로 만들었다. git 과 편집 스크립트로 복구했지만,
  **파일을 고치는 일은 반드시 스크립트 파일로** 하고 앵커가 없으면 throw 하게 짠다.
- **줄끝을 건드리지 않는다.** 이 저장소는 git 기준 **LF** 다. 편집 스크립트가 CRLF 로 쓰자
  `probe-style` 이 overlay.ts 의 CSS 를 못 찾아 스무 건이 한꺼번에 빨개졌다.
- **프로브가 눈을 대신한다.** `tools/probe-enemy.ts` 를 새로 만들어 "노란 원이 없는가"를
  도형 단위로 셌더니, 내가 안 지운 줄 알았던 HUD 엽전까지 같이 잡혔다 (그건 진짜 돈이었다).

---

### 2026-09-10 추가 28 — 새 시체 (형: "새가 죽으면 새 사체가 되어야지 왜 아직도 드론시체야")

드론을 매로 바꾸면서 **시체만 안 바꿨다.** 부러진 로터 둘과 멈춘 프로펠러 원이 그대로 돌고 있었다.

- **매** — 날개를 편 채 떨어져 **목이 꺾이고 깃이 흩어진다.** 가라앉을수록 날개가 접히고 고개가
  더 숙는다. 사람처럼 눕지 않고 팽이처럼 돌지도 않는다(각을 0.3배만 쓴다 — 그대로 쓰면 또 로터다).
  갈고리 부리 하나가 "새다"의 전부다.
- **화차** — 어제 세운 놈이라 시체가 아예 없어 **사람 시체로 눕고 있었다.** 이제 발사틀이 쪼개지고
  바퀴가 빠져 옆으로 눕고, 신기전이 있던 구멍이 타다 만 재로 남는다.

> 죽는 것마다 남는 것이 다르다: 사람은 팔다리가 늘어지고 · 보스는 무너져 납작해지며 눈이 감기고 ·
> 매는 깃을 흩고 · 화차는 부서진다. **새 적을 세울 때는 시체까지가 한 벌이다.**

---

## ★ 2026-09-10 하루 정리 — 형의 반려 스물셋

추가 22~28. 커밋 열여덟, 전부 배포됨.

| 무엇 | 어디 |
|---|---|
| 실패를 '실패'라고 크게 · 화살 규칙 · 새 살 안내 | 추가 22 |
| 초반을 많이 쉽게 (스탯 0으로 11~18판) | 추가 22 · BALANCE §9 |
| 소리를 받아 쓴다 (Freesound CC0 15종) | 추가 22 · CREDITS.txt |
| 보스 약점 넷 (눈·갑주·쌍눈·다리) | 추가 22 · RUN §3-1 |
| 때리면 반응한다 (신음·움찔·번쩍) | 추가 22 |
| 갑옷 세 벌 + 담금질 | 추가 22 · RUN §3-2 |
| 오프닝이 살아 움직인다 | 추가 22 |
| 첫 화면 문구 · 체력 바 겹침 | 추가 23 |
| 실패음 → 징 · 소리 고르기 페이지 | 추가 23 |
| PWA (홈화면 · 가로 전체화면) | 추가 23 |
| 패링 (환도) | 추가 23~26 · RUN §3-3 |
| 보스 여덟 (도깨비·구미호·장승·저승사자) | 추가 23 · RUN §3-1 |
| 패링 재작 셋 (이름·아이콘·소리·모션·자세) | 추가 24~25 |
| 손맛 렌즈가 잡은 여섯 | 추가 26 |
| 체크포인트 버그 · 걷는 보스 · 문법 치환 누락 | 추가 27 |
| 지도 = 행로도 | 추가 27 |
| 잡몹 시대에 맞게 (매·화차) | 추가 27 · RUN §3-4 |
| 새 시체 | 추가 28 |

**게이트**: 타입체크 ✓ · 테스트 **262건** ✓ · 프로브 **14종** ✓ · 빌드 ✓
· 번들 gzip 110KB / 150KB · raw 314.6KB / 400KB (상한은 형의 결정으로 올렸다 — "크기는 2순위")
· 세이브 스키마 v13 → **v16**

**★ 다음에 반드시 볼 것**
1. **챕터 3·4의 벽** (29·30판, 32~40판이 숙련 봇도 0~16%). 이번 변경 **전에도** 그랬다 —
   사수 수 × 체력이 화살 10발 상한을 넘는다. BALANCE §9-3. 별도 패스가 필요하다.
2. **소리 고르기** — 실패음·패링음 후보를 형이 고르면 그 번호로 바꾼다:
   `<배포주소>/sound-picker.html`
3. 형이 눈으로 봐야 하는 것들은 각 추가 항목 끝에 적어 뒀다.

**★ 이번에 배운 것 (다음 세션이 반복하지 않게)**
- **스크립트로 여러 파일을 고쳤으면 grep 으로 확인한다.** 편집이 중간에 실패해도 타입체크는
  "안 바뀐 코드"를 안 잡는다. 실제로 약점 문법 치환 셋이 조용히 빠져 있었다 (추가 27 ②).
- **손맛을 건드리면 그때마다 렌즈를 돌린다.** 마일스톤 끝이 아니다. feel-lens 가 눈으로는
  못 찾을 여섯 건을 잡았다 (추가 26).
- **새 적·새 물건은 시체까지가 한 벌이다** (추가 28).
- **실험장 목록이 곧 존재다.** 형은 실험장으로 확인한다 — 판에만 넣고 목록에 안 넣으면 없는 것이다.

### 2026-09-10 추가 27 — 지도 재작도 · 체크포인트 버그 · 보스가 걷는다 · 잡몹을 시대에 맞게

형의 반려 여섯을 한 묶음으로. **그중 둘은 내가 만든 버그였다.**

#### ① 체크포인트가 가본 적 없는 판을 열고 있었다 (형: "해본 적도 없는 20-1로 넘어가네")
`checkpointStage` 가 **누적 처치 수**(`bossKills`)를 받고 있었다. 10판에서 죽고 체크포인트에서
다시 시작해 10판 보스를 **또** 잡으면 누적이 2가 되어 20판이 열린다. 옛 주석의 "마디는 순서대로만
열린다"가 **같은 보스를 두 번 잡는 경우**를 안 셌다. 이제 `save.bossDepth`(가장 깊은 마디)로 센다.
세이브 **v16** — 옛 세이브는 `min(누적, 최고도달/10)` 으로 깎는다. 부푼 만큼만 깎이고 진짜로 깊이
간 사람은 안 잃는다. `bossKills` 는 활 해금의 재료로 그대로 남는다.

#### ② 약점 문법 치환 셋이 **실제 파일에 안 들어가 있었다**
추가 23 에서 "look 해석을 bossGrammar 한 곳으로 모았다"고 적었는데, 편집 스크립트가 중간에 실패해
세 군데가 옛 코드 그대로였다 — **구미호는 다리를 맞혀도 안 넘어지고, 도깨비·저승사자는 눈을 맞혀도
안 멈췄다.** 테스트가 look 0~3 만 보고 있어서 통과해 버렸다. 이제 여덟을 전부 건다 (문법·걸음 각각).
> 교훈: 스크립트로 여러 파일을 고칠 때는 **고친 뒤에 반드시 grep 으로 확인한다.** 타입체크는
> "안 바뀐 코드"를 잡아주지 않는다.

#### ③ 보스가 다 떠 있었다 (형: "왜 다 둥실둥실 떠다니냐 개빡치게")
도깨비·구미호·장승·저승사자는 **땅을 밟는 것들**이다. 저작 y 를 안 쓰고 그 자리 땅+반경이 몸 중심이며
(돌진과 같은 규칙) 걸음마다 6cm 씩 들린다. 유령 넷(눈알·갑주·쌍눈·폭주)만 뜬다 — `bossGait`.
렌더도 따라간다: 도깨비는 두 다리가 무릎에서 꺾이며 번갈아, 구미호는 네 다리가 대각선끼리,
장승은 밑동을 축으로 기우뚱, 저승사자는 도포 자락이 땅에 닿아 물결친다.

#### ④ 도깨비를 다시 그렸다 (형: "진짜 최악이야")
도깨비를 도깨비로 만드는 건 **얼굴**인데 얼굴이 없었다. 귀면와(鬼面瓦)의 문법 — 부릅뜬 두 눈,
굵은 눈두덩, 넓적한 코, 찢어진 입에 이빨 넷과 위로 솟은 송곳니 둘, 뒤로 휜 뿔, 뾰족한 귀.
몸은 곡선(어깨가 벌어지고 배가 나온다), 호피 허리춤, 채운 도형의 방망이(자루에서 머리로 굵어지고 징 다섯).
약점(이마의 큰 눈)은 scene 이 그 자리에 얹으므로 얼굴의 두 눈은 그 **아래**에 뒀다 — 힌트가 "이마의 눈"이다.

#### ⑤ 지도를 행로도(行路圖)로 (형: "지도가 너무 멋이 없으니까 지도답게좀")
문제는 **길은 있는데 땅이 없었다**는 것이다. 어두운 판 위의 동그라미 50개와 선 하나는 노선도다.
- 종이 위로: 얼룩·접힌 자국·광곽(匡郭)·그림자. 어두운 화면 속 **낡은 종이 한 장**이다 (`ui/mapart.ts`)
- 땅: 줄 사이마다 산줄기와 솔숲, 셋째 줄 아래로 물길, 아래 가장자리엔 바다, 나침반, 표제
- 길: 부드러운 곡선 + 판마다 고정 흔들림. 지나온 길은 먹, 앞길은 점선, 끝에는 **붉은 깃발**
- 칸: 마을은 원, 보스는 **붉은 인장(鬼)**, 관문은 **문(門)**, 다 딴 칸은 금빛 겹테
- 장 이름 → 지명: 一 활터 · 二 솔숲 · 三 바람재 · 四 돌다리 · 五 높은재

`tools/probe-map.ts` 신설 — SVG 는 **조용히 실패한다**(좌표 하나가 NaN 이면 그 도형만 사라진다).
DOM 을 스텁하고 1,782개 속성을 훑어 NaN·빈 값이 없는지, 판 50개·지명 다섯·표제·나침반이 있는지,
칸이 종이 밖으로 안 나가는지, 거절할 때 이유를 말하는지 본다. 프로브가 13종 → **14종**.

#### ⑥ 잡몹을 고려·조선의 것으로 (형: "드론말고 아마 새같은게")
- **매(放鷹)** 가 드론 자리를 대신한다. 날개를 치며 돌다가 **발톱의 돌을 놓는다** (예고 중엔 돌이 달아오른다)
- **화차(火車)** 신규, 26판부터 판마다 하나. **신기전이 부채꼴로 세 발** — 방패로는 다 못 막고
  **패링이 답**이다 (한 번에 쳐내면 "3발 쳐냈다!")
- 날아오는 것이 셋으로 갈렸다: 화살 · 돌 · 불꼬리 단 신기전. 죽는 소리도 갈렸다
- 실측(26~28판) 봇 클리어율 95~100% 로 그대로

#### ⑦ 새 보스 넷이 실험장 목록에 없었다 (형: "샌드박스에서 하나도 안보임")
캠페인에는 서 있었는데 소환 목록에만 안 넣었다. 형은 실험장으로 확인하므로 **형에게는 없는 것과
같았다** — 목록이 곧 존재다. 치수는 `bossStage` 의 그것과 맞췄다.

**게이트**: 타입체크 ✓ · 테스트 **262건**(+5) ✓ · 프로브 **14종** ✓ · 빌드 ✓

**형이 눈으로 봐야 하는 것**: 지도(종이·산·강·깃발이 과한가) · 도깨비 얼굴 · 넷이 걷는 모양 ·
매와 화차 · 신기전 세 발이 너무 아픈가 · 이어하기가 이제 가본 판에서 시작하는가.

### 2026-09-10 추가 26 — **손맛 렌즈를 돌렸다** (형: "하네스 스킬 다 구축해놓으면 뭐하냐고")

맞는 지적이었다. `.claude/agents/feel-lens` 를 패링에 돌렸고, 렌즈가 sim 120Hz + 렌더 60Hz
타임라인을 뽑아 **눈으로는 절대 못 찾을 것 여섯 건**을 숫자로 잡았다. 전부 고쳤다.

#### ① 납도가 226° 짜리 역슬래시였다 — 1차 반려("막대가 원을 따라 흐른다")의 진짜 원인

| 박자 | 프레임(60Hz) | 지나는 각 | 프레임당 |
|---|---|---|---|
| 발도 | 6.6 | 43° | 6.5°/f |
| 슬래시 | 4.2 | 183° | 44°/f |
| **납도** | **14.4** | **−226°** | 16°/f |

납도가 각을 a2 → a0 으로 되돌리는데 그 경로가 **몸 앞을 지난다.** 4프레임짜리 슬래시 뒤에
**더 긴 각을 3.4배 느리게 되감는 역슬래시**가 14프레임 붙어 있었다 — 스윙의 57%가 뒷정리다.
그리고 첫 칼이 늘 `up` 이라 226° 짜리 최악을 형이 제일 먼저 봤다.
→ **돌리지 않고 거둔다.** 각은 끝자세에 붙들고 길이만 줄인다. 각 복귀는 날이 4분의 1 토막이
된 뒤에만 (`SWORD.snapAt` 0.75) — 그때는 회전이 눈에 안 띈다.

#### ② 판정 창의 77%에 칼이 없었다

`active` 는 t=0 에 앵커되어 [0, 0.30] 인데 칼이 실제로 각을 지나는 건 [0.11, 0.18] 뿐이었다.
렌즈 실측: **17프레임 중 4프레임(23%)**. 화살이 5~6m 일 때 누르면 t=0.075(발도 중, 각 0%)에
쳐진다 — 화면에서는 **뒤아래로 뻗은 정지한 막대에 화살이 튕겼다.**
→ **유지(hold) 0.16초**를 넣었다. 다 벤 자세 그대로 멎는다. 야구 스윙이 스윙으로 읽히는 것도
   이 팔로스루 덕이다. 칼이 공중에 있는 구간 [0.06, 0.30] 이 판정 창을 **80% 덮는다.**
→ 반경 **2.4 → 1.8m**. 2.4면 원이 **머리 위 2.0m · 등 뒤 1.5m** 까지 덮어 칼끝이 닿는 넓이의
   3.3배였다 — "안 맞았는데 맞았다"가 나온다. 형이 "빡빡하지 않게"라고 한 건 **시간**을 달라는
   말이지 칼이 안 닿은 데서 튀라는 말이 아니다. 누를 수 있는 총 창은 0.433 → 0.400초로 거의 안 준다.

#### ③ 잘한 순간의 연출 셋이 전부 반대로 일했다
- **히트스톱 109ms** (42×2.6). 패링 직후엔 phase 가 recovering 이라 **sim 이 실제로 선다** →
  `parryLeft` 도 멈춰 칼이 그 자리에 언다. 그 자리는 대개 발도 중이다. 임팩트가 아니라 **렉**이었다.
  → ×1.7 (71ms).
- **흰 번쩍임이 칼을 덮었다.** `drawFx` 안에 있었는데 scene 은 `drawArcher` **다음에** drawFx 를
  부른다 — "맨 먼저 칠한다"는 drawFx 안에서만 참이었다. → `drawFxFlash` 로 떼어 궁수 앞에서 부른다.
  알파도 0.5 → 0.22.
- **슬로모가 가짜였다.** `fxTimeScale` 은 파티클 dt 에만 곱해진다 — 되돌아간 화살은 36m/s 로
  쌩 나는데 불꽃만 0.3초 기어갔다. "느려진다"가 아니라 "파티클이 뒤처진다"였다. → 뺐다.

#### ④ 쿨다운 0.90초 동안 화면이 아무 말도 안 했다
lockout 1.32초 중 칼이 보이는 건 0.42초(32%). 나머지 54프레임 동안 소리도 자세도 버튼 색도 없어
"게임이 씹었다"로 읽혔다. 폰 버튼은 쿨과 무관하게 켜진 색이 됐다 — **버튼이 거짓말했다.**
→ 쿨 0.55초 · `parry_fail` 사건(낮고 짧은 소리) · 버튼이 흐려진다 (`.pr-cool`).

#### ⑤ 판이 끝나면 칼이 영구히 얼었다
`world.ts` 는 `playing` 일 때만 `stepArcher` 를 돈다. 스윙 도중 판이 끝나면 `parryLeft` 가 영영
안 준다 — 결과 화면에 **활 없는 궁수가 칼을 든 채 굳어 있고**, `stepEnemyShots` 는 판 종료 후에도
돌므로 **얼어붙은 칼이 남은 화살을 계속 쳐냈다** (흰 번쩍임 + "쳐냈다!"가 결과 화면 위에서 터진다).
→ `endStage` 가 칼을 거두고, 패링 분기에 `status === 'playing'` 을 걸었다.

#### ⑥ 17ms 미만 탭이 통째로 삼켜졌다
sim 두 스텝이 한 rAF 안에서 연달아 도므로 실효 granularity 는 8.3ms 가 아니라 **rAF 16.7ms** 다.
발사에는 래치가 있는데 패링에만 없었다. → 같은 문법으로 걸었다 (`parrySeen`/`pendingParryUp`).

**게이트**: 타입체크 ✓ · 테스트 **258건**(+1: 판정 창의 75% 이상에 칼이 있는가, 반경이 칼끝을
넘지 않는가) ✓ · 프로브 13종 ✓ · 빌드 ✓

**형이 눈으로 봐야 하는 것**: 납도가 이제 "거두는 것"으로 보이는가 · 다 벤 자세로 멎는 0.16초가
어색하지 않은가 · 흰 번쩍임이 칼을 안 가리는가 · 쉬는 중에 눌렀을 때의 소리가 거슬리는가.

> **교훈**: 이 여섯 건 중 눈으로 찾을 수 있었던 건 하나도 없다. 렌즈를 안 돌린 것이 잘못이었다.
> 마일스톤이 아니라 **손맛을 건드릴 때마다** 돌린다 (CLAUDE.md 리뷰 렌즈 표).

### 2026-09-10 추가 25 — 패링 자세 재작 (형의 3차 반려)

형: **"쳐낼때는 활쏘는 캐릭터 모습은 잠시 안보여야해. 동일한 캐릭터인데 활이 안보이고 칼을
휘두르는 캐릭터가 보여야 한다고(갑옷입었으면 칼쓸때 갑자기 갑옷 없어지니 어색할테니).
환도를 허리춤에 차있잖아. 그걸 야구하듯이 허리에서 뽑아내며 휘둘러 올리고 다시 활 든 캐릭터가
보여야 한다고. 다음 타격은 내려치기가 되고 다음은 다시 올려치기가 되고."**

앞선 두 번은 **칼만** 고쳤다. 문제는 자세였다 — 활 든 궁수 위에 칼이 떠 있어 두 사람이 겹쳐 보였다.

1. **활을 든 자세를 통째로 감춘다** — 활·물린 화살·통아·호흡정지 사선이 패링 중에는 안 그려진다.
   몸·다리·머리·**갑옷**은 그대로 남는다. 같은 사람이고, 갈라지는 것은 **손에 든 것과 두 팔**뿐이다.
2. **두 팔이 칼자루를 함께 쥔다** — 먼 팔(어둡게)·가까운 팔(밝게)이 반대로 굽어 칼을 따라간다.
   팔이 안 따라가면 칼만 허공에서 도는 그림이 된다.
3. **몸이 감겼다 풀린다** — 발을 축으로 기운다 (발도에 −0.10~−0.14 rad, 슬래시에 +0.10~+0.16).
   슬래시와 **같은 감속 곡선**을 타므로 칼과 몸이 한 몸으로 움직인다.
4. **위아래 번갈아** — 첫 칼은 올려베기(발도술: 허리에서 뽑으며 앞·위로), 둘째는 내려베기
   (뒤로 넘겨 머리 위로 올렸다가 앞으로). `ArcherState.parryUp` — **sim 이 센다.**
   렌더가 세면 판을 다시 시작하거나 탭을 다녀올 때 위아래가 어긋난다 (A1).
5. **버그 둘을 같이 잡았다.**
   - 잔상 부채꼴이 **반대쪽**(칼이 안 지나간 자리)에 그려지고 있었다. 캔버스 각은 y 가 뒤집혀
     있어 월드에서 반시계로 돈 것이 캔버스에서 counterclockwise=true 인데, 부호가 반대였다.
   - 올려베기의 칼끝이 앞으로 **1px** 밖에 안 나갔다 (끝자세가 거의 수직이었다). 야구 스윙이
     아니라 제자리 회전이었다 — **프로브가 잡았다.** 끝을 앞·위로 멀리 보내 1.6m 를 가로지른다.

**프로브 (tools/probe-form.ts)** — 눈으로 고치면 재발하므로 색과 좌표로 못 박았다:
   ① 패링 프레임에 활·화살 획이 **하나도 없다** ② 칼날 획이 있다 ③ 몸은 평소의 절반 이상 남는다
   ④ 올려베기의 칼끝은 아래→위, 내려베기는 위→아래 ⑤ 둘 다 **칼 길이의 8할 이상** 앞으로 나간다
   (px 를 못 박으면 배율이 다른 판에서 거짓 통과한다 — 기준을 칼 길이로 잡았다).

| 박자 | 시간 | 60Hz 프레임 |
|---|---|---|
| 발도 | 0.11초 | 6.6 |
| 슬래시 | 0.07초 | 4.2 |
| 납도 | 0.24초 | 14.4 |
| 판정 창 | 0.30초 | 18.0 |

다시 휘두르기까지 1.32초 — 적 발사 주기(6초)의 22%다.

**게이트**: 타입체크 ✓ · 테스트 **257건**(+1: 위아래가 번갈아 나오고 판을 다시 세워도 첫 칼이
올려베기인지) ✓ · 프로브 13종 ✓ · 빌드 ✓

**형이 눈으로 봐야 하는 것**: 활이 사라졌다 돌아오는 것이 어색하지 않은가 · 두 손으로 쥔 것이
읽히는가 · 몸이 감겼다 풀리는 것이 보이는가(각이 작다 — SWORD.up/down 의 coil·follow) ·
올려베기와 내려베기가 눈에 다르게 보이는가.

### 2026-09-10 추가 24 — 패링 재작 (형의 반려 넷) · 번들 상한 해제

형: **"환도라고 하지 말고 패링이라고 하고 칼 아이콘이 있어야지. 소리도 스윙하는소리가 아니라 무슨
부메랑이 날라오는소리잖아. 모션도 박진감있게 준비자세, 발도, 빠르게 휘두르기, 이런게 순식간에
지나가야해. WWE라니까? 원에 막대가 표면타고 흘러가는 수준의 게임을 하고싶겠냐고."**
그리고: **"번들 억지로 크기 왜 자꾸 줄이냐고. 효과적인 소리 이미지 다 담으려면 크기는 2순위야."**

네 지적 다 맞았다. 특히 모션 — 예전 것은 어깨를 축으로 막대 하나가 **등속으로** 호를 돌 뿐이었다.
등속에는 무게도 의도도 없다.

1. **이름** — 버튼은 **패링**이다. 물건 이름(환도)은 세계의 말이지만 버튼에 적히는 건
   *지금 무엇을 하는가*여야 한다.
2. **아이콘** — 채운 삼각 날 + 코등이 + 자루 + 자루끝. 예전 것은 가는 선 여섯 개라 22px 로 줄면
   서로 붙어 먼지가 됐다. 아이콘은 그림이 아니라 **실루엣**이다.
3. **소리** — 1.83초짜리 스우시는 칼이 아니라 날아오는 물건의 소리가 맞다. 한 동작을 셋으로
   갈랐다: 발도(쇳소리) → 슬래시(0.65초 사무라이 베기) → 납도. **예약 시각이 그림과 같은 시계**
   (P.parry.ready/slash)를 쓴다. 전부 Freesound CC0 (public/sfx/CREDITS.txt).
4. **모션** — 세 박자 (render/stickman.ts SWORD):
   - **발도 0.11초** — 칼이 칼집에서 *자라 나오며*(길이가 0→1) 손이 허리에서 뒤·위로 감긴다.
     세제곱 감속이라 확 뽑히고 준비 자세에서 멎는다. 회전보다 이쪽이 훨씬 또렷하게 "뽑았다"로 읽힌다.
   - **슬래시 0.07초** — 186°를 네 프레임에. 2.5제곱 감속이라 **각의 대부분이 첫 두 프레임**에
     지나가고, 지나온 자리에 **채운 부채꼴** 잔상이 남는다 (선으로 그리면 또 막대 자국이 된다).
     날은 두 겹 — 강철빛 위에 흰 심 한 줄이라야 "번쩍"이 된다.
   - **납도 0.24초** — 손이 허리로 돌아가고 날이 칼집으로 빨려 든다.
   손 위치가 허리 → 뒤·위 → 앞·아래로 옮겨 다녀 **몸무게가 실린다.**
5. **성공 순간** — 화면이 한 번 하얘지고(0.05초, Fx.flash) 멈추고(히트스톱 ×2.6) 느려진다.
   불꽃 두 겹 + "쳐냈다!" + 화면 흔들림.
6. **번들 상한 110/300 → 150/400** (tools/budget.ts). 형의 결정을 주석에 남겼다. 실제로 소리·그림·
   글꼴은 public/ 이라 이 숫자에 안 들어간다 — 여기서 재는 건 JS 뿐이고, 이 상한이 진짜로 막아야
   할 것은 **몰래 들어온 라이브러리**(A6) 하나다. 지금 299.9KB / 400KB.

**게이트**: 타입체크 ✓ · 테스트 **256건**(+1: 세 박자가 갈라져 있고 슬래시가 0.1초 이하인지) ✓ ·
프로브 13종 ✓ · 빌드 ✓

**형이 눈으로 봐야 하는 것**: 발도가 "뽑는" 것으로 읽히는가 · 슬래시가 순식간에 지나가는가 ·
잔상이 과한가 · 성공했을 때 하얀 번쩍임이 눈에 아픈가(FX.flashAlpha 0.5, flashSec 0.05) ·
새 베는 소리가 부메랑이 아닌가.

### 2026-09-10 추가 23 — 환도 패링 · 보스 여덟 · PWA · 첫 화면 · 실패음 (형의 반려 아홉 건)

형: **"도장 글자가 왼쪽에 치우쳤다. 문구가 너무 허접해 — '이것은 내가 세계 제일의 궁수가 되기까지의
이야기다' 이런 식으로. '활을 든다'도 개짜치니까 '쏴라'로. 언제 꺼도 손해없다랑 소리는 M 이런건
지워라 제발. 실패 소리 개 엉망이야 — 다운받은 거 맞아? 폰 가로에서 체력바가 상단 화살UI랑 겹칠
정도로 캐릭터에서 멀어져있잖아. '환도패링' 기능을 넣어. 상단 브라우저 UI가 거슬리는데 PWA로 만들면
안되나? 보스는 왜 둥둥 떠댕기는 귀신밖에 없냐 — 거인형, 한국 전통 귀신 보스들 많잖아."**

여섯 커밋, 전부 푸시.

1. **첫 화면과 체력 바** (345f7e5) — 낙관에 `text-align: center` 가 없어 글자가 왼쪽에 붙어 있었다.
   문구는 형이 준 문장으로, 부름말은 **쏴라**, 잔소리 줄은 삭제.
   체력 바는 픽셀 하한이 **고정 72px** 이라 배율이 낮은 판(거의 언제나)에서 늘 그 값이 이겼고,
   390px 짜리 가로 폰에서는 화면의 18%라 궁수를 떠나 화살 수와 겹쳤다. 이제 하한이 화면 높이를
   따라가고(가로 폰 35px) 왼쪽 HUD 기둥 아래로 비킨다. `hudLeftBottom()` · `hpBarScreenY()` 를
   **프로브가 잰다** — 형이 두 번 지적한 문제라 눈이 아니라 숫자로 못 박았다.
2. **실패음 → 징** (2b687af) — 받은 건 맞았다. 다만 **내가 소리를 못 들어** 다운로드 수만 보고
   8비트 부저를 골랐던 게 잘못이다. 이 게임 소리는 전부 타악기(클리어=손북·보스=타이코)라
   부저는 세계 밖이었다. 후보 16종을 형이 직접 듣고 번호로 고르게 **`/sound-picker.html`** 을 붙였다
   (Freesound CDN 에서 바로 트니 게임 용량엔 안 얹힌다). 배포 주소 뒤에 붙여서 연다.
3. **PWA** (08412b0) — manifest(`display:fullscreen` · `orientation:landscape`) · 서비스 워커 ·
   아이콘 3종. 아이콘 PNG 는 `tools/make-icons.mjs` 가 **zlib 만으로 직접 인코딩**한다 (A6 — 아이콘
   한 번 굽자고 네이티브 모듈을 들이지 않는다). 워커는 문서만 네트워크 우선이라 새 배포를 안 가로막고,
   백그라운드 작업을 하나도 안 건다 (C3). 설치 안내는 게임 안에 안 넣는다 (GDD 9장) — 형이
   브라우저 메뉴에서 **홈 화면에 추가**를 한 번 누르면 주소창이 사라진다.
4. **환도 패링** (b1407c2) — docs/RUN.md §3-3. **F**(폰은 화면 버튼). 누른 순간 한 번 휘두르고,
   앞 0.30초 동안 날이 화살을 잡는다. 되돌림은 속도·방향을 **정확히 반대로** — 포물선은 시간을
   뒤집으면 왔던 길을 되짚으므로 화살이 **쏜 놈에게 그대로 돌아간다**. 조준 계산도, 누가 쐈는지
   기억할 필요도 없다. 값은 "칼을 뽑으면 당김이 풀린다"이고 화살은 안 태운다 (C2).
   허리의 칼집 · 어깨를 축으로 도는 호와 잔상 · 바람 가르는 소리 · 쇠와 쇠. 12판 힌트가 가르친다.
5. **보스 여덟** (372eca2) — docs/RUN.md §3-1. 넷은 **같은 유령 덩어리에 장식만** 달랐다.
   새로 넷: **도깨비**(거인, 방망이) · **구미호**(꼬리 아홉) · **장승**(벙거지 쓴 나무 기둥, 두들기면
   금이 간다) · **저승사자**(갓과 검은 도포). `render/bosses.ts` 신설. 한 바퀴가 여덟이라 같은 놈은
   80판 뒤에 온다. look 을 해석하는 자리를 `sim/target.ts bossGrammar` 하나로 모았다 —
   네 군데가 각자 알고 있으면 보스를 늘린 그날 셋은 맞고 하나가 틀린다.

**게이트**: 타입체크 ✓ · 테스트 **255건**(+5) ✓ · 프로브 13종 ✓ · 빌드 ✓

> ★ **번들이 꽉 찼다.** 파싱 raw **298.9KB / 300KB (100%, 여유 1.1KB)** · gzip 105.2KB / 110KB.
> 다음 기능은 상한을 올리거나(형의 결정 — C6) 덜어내야 들어간다. 형은 2026-09-02 에 이미
> "크기 꾸역꾸역 제한할 필요 없다"고 했으니 340KB 로 올리는 것이 자연스러운 다음 수다.
> 상한은 `tools/budget.ts` 한 곳에 있다.

**형이 눈으로 봐야 하는 것**: 첫 화면의 낙관과 새 문구 · 가로 폰에서 체력 바가 이제 캐릭터 곁에
붙는가 · 실패했을 때 징 소리(마음에 안 들면 `/sound-picker.html` 에서 번호로) · 환도의 칼집과
휘두르는 호가 읽히는가, 패링 판정이 너무 쉽/어렵지 않은가 · 새 보스 넷의 실루엣(특히 도깨비의
크기와 장승의 금) · 홈 화면에 추가했을 때 주소창 없이 가로로 뜨는가.

**하지 않은 것**: 보스 이미지 다운로드 — 형이 "SVG로 못 만들겠으면 다운받아와"라고 했지만,
한국 전통 귀신의 **자유 이용 스프라이트**는 여전히 없다 (엘프 때와 같은 벽). 대신 넷 다 캔버스로
그렸다 — 사진을 붙이면 스틱맨 세계와 결이 깨진다. 형이 그림을 만들어 `public/art/` 에 넣으면 붙인다.

### 2026-09-10 추가 22 — 큰 반려 한 묶음 (형: "밋밋하다·보스 형편없다·갑옷 못 고른다·피로하다" + 동생 피드백)

형: **"요즘 게임은 화려한데 우리 건 밋밋하다. 보스도 형편없고 약점 공략하는 맛도 없고, 갑옷도 못생겼고
고를 수도 강화할 수도 없다. 쉬운 것도 아니고 피로하다 — 초반을 많이 쉽게. 시작 이미지가 흥미진진해야.
사운드는 직접 만들지 말고 다운받아라. 게임은 WWE 처럼 때리면 바바바박 반응이 나와야."**
동생: **"애기살 처음 얻을 때 활성화시킨 다음 쓰라고 나오면 좋겠다. 화살 다 쓰면 실패인 걸 몰라서
보스 나올 때까지 무한 반복했다. 실패라고 큼지막하게."**

일곱 커밋, 전부 푸시. 순서대로:

1. **실패는 '실패'** (e3640bc) — 배너 큰 글자 '실패' + 사유 줄 + 어두운 띠. 첫 챕터 동안 화살 수 아래
   '다 쓰면 실패'. 1판 자막·재정비 머리에도 규칙. 처음 받은 살은 토스트 + 살통 버튼이 들 때까지 숨 쉰다
   (세이브 **v14** armedArrows). probe-ui 가 화면 폭 띠를 배경으로 안다.
2. **초반을 많이 쉽게** (65e26dc) — docs/BALANCE.md §9. 사수 체력 경사 0.25/30판 · 내 화살 피해 바닥 15
   (playerDamageFloor) · 적 화살 피해 경사 0.5/20판 (StageDef.foeDmgMul) · 첫 보스 225 · 보스 화살 +4.
   **스탯 0으로 11~18판 전부 100%** (전엔 13판부터 0~10%). 29·30·32~40판은 이전부터 숙련 봇도 0% — 숙제.
3. **소리는 받아서 쓴다** (7902756) — Freesound CC0: 활 릴리즈 3종·폭발·모루·심장·포효·패배 한 소절·신음 3종.
   합성은 파일이 없을 때의 대체. 출처와 아직 합성인 것(당김 삐걱·살별 서명·연쇄 종)은 public/sfx/CREDITS.txt.
   검색 도구는 세션 스크래치의 fs.mjs (Freesound 검색 HTML의 data-ogg 를 긁는다 — 필요하면 다시 짠다, 20줄).
4. **보스의 약점은 열렸다 닫힌다** (be1f38f) — docs/RUN.md §3-1. Target.weak/stagger/guardHits, 이벤트
   stagger·weak_shut. 눈알(뜬 눈=멈춤, 멈춘 눈=×1.5) · 갑주(막힌 몸통 둘=투구 열림) · 쌍눈(엇박, 한쪽=둘 다 멈춤) ·
   폭주(다리=넘어짐). 금빛 고리·별 셋·기울기, 쿵/미끄러짐, 힌트 4종. tests/boss-weak.test.ts 5건.
5. **때리면 반응한다** (ef9d5c1) — 신음(실사운드)·몸이 젖혀지는 움찔·흰 번쩍·헤드샷 시체 ×2.4 날아감·
   사람/귀신 명중 흔들림 ×1.5, 머리 ×2, 멈춤·세게 죽음은 폭발만큼.
6. **갑옷 세 벌** (112263e) — docs/RUN.md §3-2. game/armor.ts. 가죽갑(처음부터)·두정갑(40)·찰갑(120)을 출정에서
   고르고, 대장간에서 담금질(3단, +20%/단, 값은 활 개조 곡선). 판 도중의 '갑옷'은 입은 벌. 세이브 **v15**
   (옛 세이브는 두정갑 그대로). 그림: 가죽=박음질+X끈 · 두정=쇠징 · 찰갑=쇠비늘+붉은 끈 · 담금질=금테.
7. **오프닝이 살아 움직인다** (df1391a) — 켄 번즈·불티 14·화살 줄기 3·제목 글로우·낙관. CSS 뿐 (rAF 없음).

**게이트**: 타입체크 ✓ · 테스트 **250건** ✓ · 프로브 13종 ✓ · 빌드 ✓ · 번들 gzip **101.2KB / 110KB** (여유 8.8KB).

**형이 눈으로 봐야 하는 것**: 실패 배너의 띠·크기 · 살통 버튼의 숨 쉬는 테 · 보스 눈꺼풀이 감기는 게 읽히는가 ·
멈춘 보스의 금빛 고리·별 · 넘어진 폭주귀신의 기울기 · 움찔·번쩍이 과한가 · 갑옷 셋의 그림(특히 찰갑 비늘이
스틱맨 위에서 읽히는가) · 오프닝 불티·화살 줄기의 양 · 새 소리들(릴리즈 3종이 합성보다 나은가, 신음이 거슬리는가).

**하지 않은 것 / 다음**:
- **그림 생성** — 형: "챗지피티 같은 걸로 브라우저에서 이미지 만들어 올 수도". 연결된 크롬은 다른 기기라
  파일이 그쪽에 떨어진다. 형이 자리에 있을 때 시도하거나, 형이 만들어 public/art/ 에 넣으면 붙인다.
- 챕터 3·4(29~40판)의 벽 (BALANCE.md §9-3). 봇이 눈·통을 못 노려 보스판·화약판은 사람이 판정.
- 당김 삐걱·긴장음은 여전히 합성 (연속값 반응이라 파일로는 안 된다 — CREDITS.txt).

### 2026-09-03 추가 21 — **소리가 또 안 났다** (형: "또 소리가 안난다")

두 번째 반려다(첫 번째는 2026-08-31, 추가 15-3). 그때는 제스처를 캔버스에서만 들어서 못 잡았고,
이번에는 **잡고 나서 놓쳤다.**

**원인.** `ctx.resume()` 은 **비동기**다. 사용자 제스처 안에서 불러도 브라우저가 거절하거나
(정책·기기·bfcache 복귀) 늦게 풀릴 수 있다. 그런데 `game/loop.ts` 의 문지기는 첫 눌림 한 번에
`unlockSfx` 를 부르고 **곧바로 리스너를 떼어버렸다.** 그 resume 이 실패하면 synth 는 만들어졌는데
`ctx.state` 가 영원히 `'suspended'` 로 남고, `synth.ts` 의 `synthLive()` 가 false 라서
**그 세션의 모든 소리가 아무 말 없이 사라진다.** 다시 열 기회는 없다 — 문지기가 이미 갔으니까.

**고친 것 셋.**

1. **문지기가 소리가 진짜 살아날 때까지 남는다** (`game/loop.ts`).
   `if (sfxLive(sfx)) return; unlockSfx(sfx)` — 살아 있으면 즉시 되돌아가므로(필드 두 개 읽기)
   매 눌림의 비용은 사실상 0이고, 실패했으면 **다음 누름이 다시 연다.** 음소거 중이면
   `ensure` 가 알아서 되돌아간다 — 형이 끈 것을 여기서 켜지 않는다 (C3).
2. **"열려고 했다"와 "열렸다"를 구별한다.** `synth.ts` 의 `live()` 를 `synthLive()` 로 내보내고,
   `sfx.ts` 에 `sfxLive()` 를 얹었다. `unlocked` 플래그는 의도이고 이건 사실이다.
3. **화면이 사실을 말한다** (`render/hud.ts` `hud.silent`). 음소거가 아닌데 1초 넘게 소리가
   잠겨 있으면 오른쪽 위에 **"소리 잠김 · 화면을 한 번 누르면 켜짐"**. 자동재생 정책은 조용히
   실패하므로, 아무 말도 없으면 게임이 고장 난 걸로 보인다. 이제 화면만 봐도 셋이 갈린다 —
   `무음 · M`(형이 끈 것) / `소리 잠김`(브라우저가 막은 것) / 아무 말 없음(정상).

**프로브** `tools/probe-sound.ts` (신규, `npm run probe` 편입). AudioContext 를 **상태 기계로**
스텁해 소리가 열리는 순간만 잰다: ① 제스처 전엔 컨텍스트를 안 만든다 ② 창 어디를 눌러도 열린다
③ **첫 resume 이 거절당해도 다음 제스처가 되살린다**(이번 버그) ④ 음소거로 시작해도 M 한 번에
열린다 ⑤ 탭 복귀 ⑥ pagehide 뒤 복귀. 13항목 전부 통과. probe-audio.ts 는 "무엇이 예약되는가",
이쪽은 "예약할 수 있는 상태인가" — 둘이 붙어야 소리가 난다.

> **그래도 조용하면 순서대로 본다.** ① 오른쪽 위에 `무음 · M` 이 있나 → M 한 번.
> ② `소리 잠김` 이 있나 → 화면을 한 번 더 누른다. ③ 아무 말도 없는데 조용하다 →
> **아이폰 측면 무음 스위치**(벨/무음)를 확인한다. 사파리는 그 스위치에 WebAudio 를 같이 묶는다.

### 2026-09-03 추가 20 — 첫 화면의 말과 **가로 안내** (형의 요구 2건)

형: **"첫 실행 화면에서 게임을 가로로 플레이하라는 애니메이션 떴으면 좋겠다. 첫 화면 문구도 바꾸고."**

1. **가로 안내** (`ui/opening.ts` `.op-turn`) — 오프닝 아래에 알약 하나. 금색 테두리의 폰이 3.4초마다
   **세로 → 가로로 눕고**, 눕힌 채 멈췄다가 사라진다 (되돌아가는 회전은 안 보여준다 — 왕복시키면
   "다시 세우라"로 읽힌다). 화면 속 붉은 가로선이 곧 화살길이라, 눕는 순간 길이 길어진다.
   - **세로일 때만 뜬다.** `matchMedia('(orientation: portrait)')` 를 듣고 있어서, 형이 폰을 돌리면
     그 자리에서 사라진다 — 사라지는 것 자체가 "됐다"는 신호다. 이미 가로면 처음부터 안 뜬다.
   - **막지 않는다.** 세로로도 게임은 된다 (camera.ts portraitAspect). 안내 위를 눌러도 그냥 시작한다 (C1).
   - `prefers-reduced-motion` 이면 회전을 끄고 누운 상태로 세워 둔다.
2. **첫 화면 문구** — 설명 대신 이 게임의 서명을 앞세웠다.
   "**마지막 한 발에, 시간이 멎는다**" (MEGAHIT 한 발) + 작은 줄로 "활 한 번 안 잡아본 스틱맨이
   신궁이 되기까지 · 한 판 30초". 기록 없을 때는 "첫 여정" → "**아직 한 발도 쏘지 않았다**".
   맨 아래 줄에 C2를 한 마디로 심었다: "아무 데나 누르면 시작 · **언제 꺼도 손해 없다** · 소리는 M".
3. **프로브** (`tools/probe-opening.ts`, `npm run probe` 에 편입) — DOM 스텁으로 오프닝을 실제로
   마운트해 ① 문구가 있는지 ② 세로에서만 안내가 붙고 돌리면 떨어지는지 ③ 시작이 정확히 한 번이고
   리스너를 다 떼는지를 잰다. 16항목 전부 통과.

### 2026-09-03 추가 19 — 오프닝 · 갑옷 입은 모습 · 대장간 그림과 망치 · 부적 그림 · **언덕과 골짜기** (형의 요구 6건)

형: **"갑주 입으면 입은 모습으로 보여야지. 오프닝 화면을 띄워줘, 바람의나라 느낌처럼. 대장간도 대장간스러운
이미지랑 강화 시 망치질 소리. 부적도 부적마다 이미지 다르게. 게임 배경 더 강화하고 언덕이랑 높낮이차
이런 것 있는 스테이지들 구현해야지."**

1. **오프닝** (`ui/opening.ts`) — 켤 때 한 번. 무용총 「수렵도」 위에 神弓 / 신궁, 기록 한 줄, 깜빡이는
   "활을 든다". 아무 데나 누르거나 아무 키(M 제외)면 그 제스처로 루프가 선다 (소리의 열쇠이기도 하다).
   탭 복귀에는 안 뜬다 — 그건 시작이 아니라 복귀다 (C1).
2. **갑옷 입은 모습** (`render/stickman.ts` ARMOR_PLATE) — 두정갑을 입으면 어깨~골반 천 판 + 쇠징 3×3.
   깎일수록 흐려지고 0이면 사라진다 — 벗겨지는 중임이 보여야 산 값을 안다.
3. **대장간** — 머리에 김홍도 「대장간」, 개조를 누르면 망치질 두 번(`sfx 'forge'`: 사각파 하강 + 쇳가루
   노이즈 + 모루 울림). 스탯 올리기(levelup)와 소리가 다르다 — 다른 일을 했으니 다른 소리다.
4. **부적 그림** — 노란 종이에 붉은 글자 箭·甲·金·鬼 (`ui/arrowicons.ts charmIconSvg`, 인라인 SVG).
   재정비 머리는 수렵총 벽화로 바꿔 화면마다 그림이 다르다 (오프닝 무용총 · 출정 활쏘기 · 재정비 수렵총 · 대장간).
5. **★ 지형** (`sim/terrain.ts`) — 판이 땅의 높이를 **꺾은선**(`StageDef.ground`)으로 적는다. 규칙:
   궁수 발밑은 언제나 0 · 저작 y는 **그 자리 땅에서 잰 높이** · 땅에 닿는 것(내 화살·적 화살·떨어지는
   과녁·달려오는 사람·시체)은 전부 `groundAt` 하나를 본다. 렌더도 같은 함수로 땅을 꺾은선으로 채우고,
   그림자·풀·기둥·깃대·건물이 그 자리 땅에 선다. 카메라는 언덕 위 과녁과 봉우리까지 구도에 넣는다.
   - 캠페인 셋이 언덕/골짜기가 됐다: **1-6**(언덕 위를 겨눈다) · **2-8**(언덕 위의 높은 연쇄) ·
     **3-6**(골짜기 건너로 바람). 무한 구간에 테마 둘 추가: **언덕**(봉우리 1~2) · **골짜기**(땅이 꺼진다).
   - 실측: 1-6 첫 판(봉우리 2.6m)은 average 봇 73% — 평탄한 궤적이 언덕에 긁혔다. 봉우리를 1.9m로
     낮추고 뒤 과녁을 띄우니 전 봇 100% (근력 8 · 스탯 0 모두). 2-8 90/100 · 3-6 100/100.
   - 회귀 하나 잡음: 돌진에 넣으려던 "땅을 따라 오른다"가 **보스 분기에 먼저 매칭**돼 보스가 0.6m
     내려앉았다 (화전 테스트가 잡았다). 보스판은 평지라 baseY 그대로.
   - tests/terrain.test.ts 5건: 보간 · 궁수 발밑 0(캠페인+무한 두 바퀴) · 언덕 위 과녁 높이 · 화살이 언덕에
     꽂힘 · 돌진이 언덕을 오름.

**게이트**: 타입체크 ✓ · 테스트 **238건**(+6) ✓ · 프로브 ✓ · 빌드 ✓ (gzip 95.6KB+ / 110KB).
**형이 눈으로 봐야 하는 것**: 오프닝 글자 크기(폰 56px) · 갑옷 판이 팔에 가려 읽히는가 · 1-6·2-8·3-6의
언덕 모양과 땅 색 · 무한 구간 '언덕'·'골짜기' 판의 카메라(봉우리가 잘리지 않는가) · 망치 소리 크기.

**엘프**: 판타지 그림은 자유 이용 소스가 없어 못 넣었다. 형이 노트북에서 만들어 `public/art/`에 넣으면 붙인다.

---

### 2026-09-03 추가 18 — 진짜 그림 둘 · 쉬운 말 (형의 요구 2건)

형: **"게임 분위기에 적절하면서도 수려한 이미지도 확실한 부분에 적절하게 집어넣는 게 필요해. 엘프라던지.
그리고 용어 너무 전문적이면 사람들이 뭔지 못 알아먹으니까 좀 적당히 써."**

1. **그림** — 새로 그리지 않고 국궁을 그린 **진짜 그림**을 가져왔다 (`public/art/출처.txt`):
   출정 화면 머리에 김홍도 「활쏘기」(퍼블릭 도메인), 재정비 화면 머리에 고구려 무용총 「수렵도」
   (공공누리 1유형). 위키미디어 원본(3.5MB·7.2MB)을 PowerShell(System.Drawing)로 1100·1400px, 201·369KB로
   줄였다. 패널이 열릴 때만 받고, 번들 예산 밖이다. 그림 이름은 구석에 작게 적는다(출처 표시 의무).
   **엘프 같은 판타지 그림은 자유 이용 소스가 없다** — 형이 노트북에서 만들어 `public/art/`에 넣어 주면
   같은 자리에 붙인다 (이 PC엔 크롬 확장이 없어 ChatGPT 생성물을 여기로 못 가져온다).
2. **쉬운 말** — 이름은 쉬운 말이 앞, 활 용어는 괄호: 시위→**활줄**(시위), 활채→**활몸**(활채),
   줌통→**손잡이**(줌통), 두정갑→**갑옷**(두정갑), 파귀 부적→**귀신잡이 부적**, "만작"→"끝까지 당김",
   편전·전쟁화살은 궁합 줄에서 괄호로. 국궁 용어를 버린 게 아니라 **뒤로 보낸** 것이다.
3. 헤드리스 프로브가 `import.meta.env` 없이도 돌게 `BASE`에 가드를 걸었다 (ui/loadout.ts · growth.ts).

**게이트**: 타입체크 ✓ · 테스트 232 ✓ · 프로브 ✓ · 빌드 ✓ (gzip 93.9KB / 110KB)
**형이 눈으로 봐야 하는 것**: 출정·재정비 머리 그림의 어둡기(글자가 읽히는가) · 폰에서 그림 높이 ·
값이 오른 뒤 부적·개조를 "살 만하다"고 느끼는가.

---

### 2026-09-02 추가 17 — 경제 실측: "10분에 1000" · 비용 곡선 전면 개정 (형의 반려)

형: **"다 너무 싼 거 아냐? 컨텐트 소모시간이 너무 빨라버리는 거 같은데 제대로 된 거 맞는지 파악해."**
**"내가 한 10분 플레이하면 1000 모은단 말이야."**

맞았다. `tools/probe-economy.ts`(실제 채점기 gradeRun에 실제 판을 넣는다)로 재니 숙련자는 **10분에
1,263** — 형의 체감과 같다. 그 수입으로 오전의 값을 사면 4스탯 14 + 주활 개조(핵심 영구 세트 776)가
**6분**에 끝났다. 비용식이 제곱뿐이라 14레벨이 25였고, 그건 판 하나 수입이다.

바꾼 것 (docs/BALANCE.md §9): 스탯 비용식에 **3차 항**(costCubeDiv 14)을 더해 뒤만 가파르게 —
L5 19 · L10 99 · L14 244 · L20 659. 개조 60·220·540, 부적 35~60, 화살 12→24→36, 가게 10·15·20·30.
결과: 숙련 핵심 세트 **46분**, 전부 약 **10시간**. 초보는 첫 여정에 근력 **7**(전 11) — 여자친구
시나리오(§8 근력 8이면 11~19판이 열린다)는 한 바퀴 더 벌면 그대로 성립한다.

**안 바꾼 것**: 숙련의 수입. 빨리 깨는 것이 보상인 건 MEGAHIT §1의 결정이다. 그래서 숙련자는 여전히
초보의 1/5 시간에 모든 것을 산다 — 값으로는 못 고치는 격차라 §9-3에 그대로 적었다.

---

### 2026-09-02 추가 16 — 훈련치를 쓰는 곳 다섯 · 금관 사수 · 상한 110KB (형: "돈을 벌고 싶어지도록")

형: **"상한 올리면 뭐 어때서. 올려. 죽고나서 재정비 잘 뜬다. 그리고 돈쓸곳을 좀더 많이 마련해봐야해.
돈을 벌고 싶어지도록. 더 창의적이고 확실한 재미를 주는 요소들도 깊게 더 고민해봐."**

진단은 docs/GAP.md §9에 적었다 — 소비처가 스탯 하나에 몰려 있어 **지갑이 닫히는 날**이 있었고,
실력이 돈으로 바뀌는 **버는 사건**이 판 안에 없었다. 축이 전부 다른 자리 다섯을 열었다.

1. **대장간** (`game/forge.ts`) — 활 개조, **활마다** 따로, 영구. 시위(만작 시간 −7%/단) ·
   활채(초속 +3.5%/단 → 피해는 제곱이라 +7%) · 줌통(떨림 −10%/단), 각 3단, 값 6·12·18.
   효과는 `bowMods`의 마지막 곱으로만 들어간다 — sim은 대장간을 모른다. 성장 화면(활 걸이 아래)과
   재정비 화면 둘 다에 선다 (`ui/growth.ts buildForgeRows`).
2. **부적** (`game/charms.ts`) — 출정 때 **하나만**, 이번 여정만. 살통(매 판 화살 +1, 8) ·
   철갑(두정갑 입고 출발, 10) · 노다지(훈련치 ×1.3 · 체력 −30 출발, 7) · 파귀(보스 체력 ×0.75, 12).
   값은 '나선다'를 누르는 순간 치른다. 효과는 전부 game 레이어 판 경계에서 (A1). 세이브 `runCharm`.
3. **화살 한 발** (`game/defense.ts` 셋째 항목) — 판 도중 장터 버튼줄. 4 → 7 → 10, 판당 셋.
   `granted`도 같이 올려 소모 계산이 안 틀어진다. `DefenseState.arrowsBought`.
4. **금관 사수** (`TargetSpec.bounty`) — 12판+ 창의 사수(look 1, 갑옷 아님) 중 하나가 판의 40%에서
   금관을 쓴다 (판 번호가 시드 · 앞의 난수를 안 건드린다). **머리를 맞혀 즉사시키면** sim이
   `bounty` 사건을 뱉고 채점에 현상금 +8이 붙는다 (`rewards.ts FEAT_BOUNTY`, 죽어도 가져간다).
   몸통으로 눕히면 아무것도 없다 — 현상금은 처치가 아니라 **조준**의 값이다. 렌더는 머리 위에
   금관(`render/foe.ts`), 소리는 두 음 종(`audio/sfx.ts`), 힌트에 "금관의 머리엔 현상금".
5. **살 가게** (`game/supply.ts shopPrice`) — 출정 화면. **발견한**(arrowStock에 키가 있는) 특수살만,
   처음 나오는 마디로 값을 매긴다 (3·4·5 · 신전 7). 가게가 보급을 대신하면 보스를 잡을 이유가 줄어서다.

**상한**: C6을 **gzip 110KB / raw 300KB**로 올렸다 (형의 결정). 지금 93.2KB / 259.1KB.
**아이콘**: game-icons.net CC BY 3.0 넷 추가 (anvil · rune-stone · coins · crown, `public/icons/출처.txt`).
**세이브 v13**: `forge` · `runCharm`. 옛 세이브는 빈손으로 올라온다.

**게이트**: 타입체크 ✓ · 테스트 **232건**(+16) ✓ · 프로브 11종 ✓ · 빌드 ✓ · 밸런스(11~20판 근력 8) 변화 없음 —
금관·가게·부적은 봇이 안 쓰므로 표가 그대로다. 경제표는 docs/BALANCE.md §9.

**형이 눈으로 봐야 하는 것**: 출정 화면이 길어졌다(활 · 부적 · 살 가게) — 폰 세로에서 '나선다'까지
스크롤이 자연스러운가 · 금관이 창문 사수의 머리 위에 제대로 앉는가(각도가 기울 때) · 장터 버튼 셋이
폰에서 궁수를 안 덮는가 · 재정비 화면에 대장간 셋이 더 붙어 길어진 것이 부담스럽지 않은가.

**하지 않은 것**: 부활(아처로식 "한 번 더") — RUN.md "완전 게임오버"에 걸려 형의 결정을 기다린다.
헤드샷 봇이 없어 금관의 실제 획득률은 사람이 잰다.

---

### 2026-09-02 추가 15 — 재정비: 죽은 자리가 곧 강화 · 11판의 벽 실측 (형의 여자친구가 접었다)

형: **"여자친구가 해봤는데 11까지 가고 어려워서 안하겠대. 아마 강화하는걸 안해봤나봐.
죽고나서 강화하는거 바로화면에 띄워줘야 하지 않을까? 하단에 '다음'버튼도 만들어서 활 고르기로
가서 고른다음 시작하게 하고 말이야. 그리고 진짜 상업성 있게 대규모 업데이트좀 해줘."**

1. **재정비 화면** — 여정이 끝나면 **결과 머리 + 성장 줄 넷 + '다음 →'** 이 한 장이다
   (`ui/growth.ts showReinforce`). 다음은 출정(활 고르기)이고, 활이 하나뿐이면 곧 시작이다.
   옛 "같은 활로 다시 / 채비 바꾸기" 화면(`showRunOver`)은 지웠다. 머리에 **"다음 여정은 N판부터"**
   를 적는다 — "또 1판부터냐"가 접는 이유가 되지 않게. 출정 화면에도 `출발 N판`이 붙었다.
   성장 줄은 Tab 성장 화면과 **같은 코드**(`buildStatRows`)다.

2. **추천 줄** (`progression.recommendStat`) — 진짜 만작(근력 14) 전까지는 언제나 **근력**,
   이유 한 줄("화살 피해는 속도의 제곱이다. 적을 눕히는 발수가 준다")이 그 줄에 붙는다.
   만작이 열리면 가장 낮은 스탯으로 넘어간다. 화면을 가리는 손가락·화살표는 없다 (GDD 9장).

3. **첫 성장 안내** — 처음 올릴 수 있게 되는 판에 토스트 한 줄, 딱 한 번 (세이브 **v12**
   `seenGrowHint`). HUD 성장 버튼의 점은 이제 **숨 쉰다**(CSS 펄스) — 가만한 점은 처음 보는
   사람에게 장식이었다. `ui.toast`가 ms를 버리던 배선도 고쳤다.

4. **★ 11판의 벽 — 실측.** 밸런스 시뮬이 11판 이후를 **저작 원본(과녁)** 으로만 돌려서, 실제
   판(사수 체력 70 · 나를 쏜다)을 한 번도 잰 적이 없었다. 실제 판으로 돌리니 **12판은 숙련 봇도
   0%** — 100% 명중으로 화살을 다 써도 사수가 선다. 원인: 피해 = 40 × (속도/60)², 근력 0의
   몸통샷은 **12**(사수 하나에 여섯 발). 형의 짐작("강화를 안 해봤나봐")이 숫자로 맞았다.
   → `P.enemy.convertHpEase`(0.5): 11판 사수 체력 35에서 10판에 걸쳐 70으로. 근력 0도 세 발,
   근력 8 두 발, 만작 한 발. 전환 판 화살은 **사수 하나에 한 발씩**. 경사 후 근력 8이면 11~19판
   전부 열린다 (초보 봇 78~100%). 표·이유는 docs/BALANCE.md §8, 격차 분석은 docs/GAP.md §8.
   시뮬 새 옵션: `--real=0` · `--zero=1` · `--stats=str:8` · `--from/--to`, 표에 `death` 열.

5. **사수 판의 힌트** — "공중 과녁은 맞으면 떨어진다"가 드론 앞에 뜨고 있었다. 11판:
   *"적이 활을 든다 — 당기는 쪽을 먼저 쏜다. 머리는 한 발이다"*, 그 뒤는 구성(창·숨는 사수·
   드론)이 말한다. 화약통 판은 저작된 말을 그대로 둔다.

**게이트**: 타입체크 ✓ · 테스트 **216건**(+8) ✓ · 프로브 11종 ✓ · 빌드 ✓
번들 gzip **89.5KB / 90KB** (여유 0.5KB — 옛 종료 화면을 지우고도 이렇다. **다음 기능 전에
반드시 덜어내거나 C6 상한을 다시 의논해야 한다.** 형은 이미 "크기 꾸역꾸역 제한할 필요 없다"고
했으니 100KB로 올리는 것이 자연스러운 다음 수다.)

**형이 눈으로 봐야 하는 것**: 죽은 뒤 재정비 화면 — 머리(판 수·가져간 것·다음 여정 시작 판)와
성장 줄, 추천 꼬리표의 색, 폰 세로에서 '다음 →' 버튼이 스크롤 없이 닿는가 · 성장 버튼 점의
펄스가 거슬리지 않는가 · 11판 사수가 근력 0 세 발에 눕는가.

**하지 않은 것 (형의 결정이 필요)**: ① 훈련치로 그 자리에서 되살아나는 '한 번 더'(아처로식
부활) — RUN.md의 "완전 게임오버"를 건드리는 일이라 넣지 않았다. ② 보스판(10·20·30)의 초보
클리어율은 봇이 머리를 못 노려서 이 계측기로 못 잰다. ③ 21~30판(이동·바람)은 봇 명중률 자체가
50~75%라 별도 숙제.

---

### 2026-09-01 추가 14 — 판 도중에 사는 방어 · 화살은 적보다 한 발 많다 (형의 반려 2건)

1. **방패(防牌)와 두정갑(頭釘甲)을 판 도중에 산다** (형: "게임플레이 도중에 방어벽이나
   방어구 구매하게 해달라 그랬는데 아직 구현이 안됐네").

   그동안 이 게임에는 **맞는 것에 대한 대답이 없었다.** 적 화살이 오면 할 수 있는 건 셋뿐이고
   (먼저 쏴 죽인다 · 맞불로 쳐낸다 · 과녁 뒤에 선다) 셋 다 **판의 배치가 허락해야** 되는
   것이라, 배치가 나쁜 판에서는 선택지가 하나도 없었다. 사는 것이 네 번째 답이다.

   둘의 성격을 일부러 갈랐다 — 값이 같으면 하나가 다른 하나의 열등한 사본이 된다:

   | | 지속 | 값 | 하는 일 |
   |---|---|---|---|
   | **방패** | **이 판만** (판이 끝나면 사라진다) | 훈련치 3 | 앞에 세운다. 적 화살 4발을 삼킨다 |
   | **두정갑** | **여정 내내** (판을 넘어 이어진다) | 훈련치 6 | 입는다. 피해 50을 대신 받는다 (상한 100) |

   - **상점 화면을 안 만들었다.** 화면을 열면 판이 멈추고, 판이 멈추면 "지금 위험하니까
     산다"는 이 기능의 유일한 이유가 사라진다. 살통(`ui/quiver.ts`)과 **같은 문법**의
     HUD 버튼 둘이다 — 누르면 그 자리에서 선다.
   - **파는 것은 훈련치뿐이다** (GDD 9장 — 결제·광고·에너지 없음). 스탯을 올리는 그 지갑이라
     "지금 방패를 살까, 힘을 올릴까"가 진짜 저울질이 된다.
   - **방패는 몸을 통째로 덮는다** (윗변 2.0m). 처음에 손 높이(1.25m)로 잡았다가 테스트에서
     잡혔다 — 피격 판정은 손 1.4m ± 0.55라 위로 1.95m까지다. 낮으면 화살이 어깨 위로 들어와
     "세웠는데 그냥 맞는" 물건이 된다. 내 화살은 널판 가운데의 **사격구**로 나간다.
   - **나를 깎는 길을 한 문으로 모았다** (`sim/target.ts hurtPlayer`). 적 화살과 돌진 접촉이
     각자 `w.hp`를 깎고 있었는데, 그 상태로 갑옷을 붙이면 반드시 한쪽에만 붙는다.
   - 남은 양은 **화면이 말한다**: 방패에 삼킨 발수만큼 화살이 꽂히고(`render/scene.ts`),
     두정갑은 체력 바 **위에** 얇은 한 줄로 얹힌다 — 체력 바를 나눠 쓰면 벗겨질 때
     체력이 준 것처럼 보인다.
   - 아이콘 둘은 기성 CC BY 3.0 (game-icons.net · lorc/arrows-shield · lorc/layered-armor).

2. **화살은 적보다 반드시 한 발 많다** (형: "어떤 상황에서도 게임 시작했을 때 화살 수는
   아무리 적어도 적 수보다 1개 더 많아야 해").

   화살이 적보다 적어지는 길이 셋 있었고 **셋 다 의도된 설계**라 아무도 버그로 신고하지
   않았다: ① 화약 상자 퍼즐판(정답 발수만 줬다 — docs/GAP.md 1절) ② 갈림길 '단발' 카드
   ③ 화살 보유가 바닥난 사람(`grantArrows`의 최소 지급). 겹치면 다섯이 선 판에 두 발이 된다.

   바닥은 **판을 내보내는 마지막 문**에 건다 (`game/stages.ts getStage`) — 저작 40판 ·
   무한 생성 · 보스 · 11판+ 전환 중 어느 길로 만들어졌든 전부 이 문을 지난다. 각자의 자리에
   따로 걸면 반드시 하나를 빠뜨린다. 지급하는 쪽(`grantArrows`)에도 같은 함수를 한 번 더 건다.
   세는 법은 `game/stagekit.ts arrowFloor` 하나뿐이고, "적"의 정의는 `anyTargetStanding`과
   **정확히 같다** (보급·화약 상자는 클리어 조건이 아니므로 적이 아니다).

   실측: 1~200판 중 실제로 올라간 판은 **1-9 하나(3발 → 4발)** 뿐이다. 나머지는 이미
   바닥 위에 있었고, 상한 10을 넘긴 판은 0개다. 퍼즐은 그대로 퍼즐이다 (적+1은 한 발밖에
   못 빗나가는 수다) — 사라진 건 **수학적으로 이길 수 없는 판**뿐이다.

**게이트**: 타입체크 ✓ · 테스트 **208건**(+17) ✓ · 프로브 11종 ✓ · 빌드 ✓
번들 gzip **88.2KB / 90KB** (여유 1.8KB — 빠듯하다, 다음에 뭘 넣기 전에 한 번 덜어내야 한다).

**형이 눈으로 봐야 하는 것**: 방패를 세웠을 때 널판이 궁수를 가리지 않는가(궁수 x=0,
방패 x=1.26~1.94m) · 사격구가 손 높이에 보이는가 · 버튼 두 개가 늘어난 아래 조작 바가
폰에서 궁수를 안 덮는가.

---

### 2026-08-31 추가 13 — 척후는 사람이다 · 소리 · 창 · 집중 철회 (형의 반려 6건)

1. **척후(斥候)가 칼을 든 사람이 됐다** (형: "척후는 비행체가 아니라 칼을 들고 나에게
   달려오는 사람모습이어야해. (…) 비행체는 죽을때 사람시체로 쓰면 안돼. 비행체 잔해여야지").
   그전 돌진은 **왼쪽을 가리키는 삼각형 + 꼬리 둘**이었는데 죽으면 **사람 시체**가 남았다 —
   본 것과 남는 것이 서로 다른 종이었다. 이제 앞으로 기운 몸 · 엇갈려 뛰는 다리 ·
   치켜든 쇳빛 칼로 그린다(`render/foe.ts drawFoeRusher`).
   그리고 **땅을 딛는다**: 돌진의 중심 높이는 저작이 아니라 **반경**이 정한다(sim/world.ts) —
   그전엔 1.3~3.2m 상공이라 어떻게 그려도 떠 있는 물건이었다. 사람 반경 하한도 같이 건다.
   ★ 남긴 규칙: **화면에 보이는 것과 죽어 남는 것이 같은 종이어야 한다** (tests/rusher.test.ts).

2. **창문이 사람보다 작지 않다** (형: "가끔 창문이 너무 작아서 적이 거의 안보이는 경우 있는데
   (…) 최소 적군사람 머리랑 상체 나올만큼은 크게"). 창 크기는 사수 반경에서 나오므로
   반경이 작으면 창도 사람도 같이 작아진다. 프로브가 재보니 **9/40판의 창 높이가 0.77~0.90m**
   였다 — 상체가 들어갈 수 없는 크기다. `P.enemy.foeMinR = 0.5` 로 하한을 걸어 전부 1.05m 이상.
   (`tools/probe-foe.ts` 가 40판을 표로 잰다.)

3. **소리가 안 나던 것** (형: "왜 소리가 안나는지 모르겠다"). 자동재생 정책상 첫 제스처가
   있어야 AudioContext가 열리는데, 그 제스처를 **캔버스에서만** 듣고 있었다. 폰에서 첫 터치는
   거의 언제나 **DOM 버튼**(아래 조작 바·갈림길 카드·성장 화면)이라 캔버스에 안 닿고,
   그러면 그 세션의 소리가 통째로 조용히 사라진다. 이제 **창 전체에서 캡처 단계로** 듣는다.
   → 그래도 조용하면 성장 화면의 '소리 (M)' 체크를 봐라. M 키로 한 번 끄면 그 상태가 저장된다.

4. **만작에 '삥' 소리** (형: "최대로 당겨졌을때 (…) 동시에 소리도 뭔가 작게 '삥' 하는 소리").
   기존 만작음은 155Hz 45ms 짜리 저역 "툭"이라 **폰 스피커에서 거의 안 들렸다.** 화면은 이미
   반짝이는데(rig.flash) 귀에는 아무것도 안 왔다. 그 섬광과 같은 순간에 2100Hz 85ms 맑은 한 톨을
   얹는다. 작게 — 크면 만작이 상으로 들려 매번 끝까지 끌게 된다.

5. **집중(集中) 게이지를 걷어냈다** (형: "당기는 힘 떨어지는 바 밑에 뭔가 하나 바가 더 생겼는데
   그거 필요없을듯. 지워버려" · "집중이 원래 기존 숨참기 아니었어?? 왜 들어간거야 지워").
   같은 날 아침에 넣은 것을 같은 날 저녁에 뺐다. 이유가 옳다 — **'집중'은 이미 스탯의 이름**
   (숨참기 한계)이었고, 같은 이름의 물건이 둘이면 배우는 게 아니라 헷갈린다.
   막대 · 탄도 보정 · 소리 · 노브(P.focus) · 프로브 · 테스트까지 통째로 뺐다.
   발사각은 다시 **겨눈 방향 그대로**다. 간극은 docs/GAP.md 6절에 열린 채로 남겼다.

6. **화면 하나가 닫혀도 게임이 안 굳는다** (형: "게임오버됐을 때 창 x 눌러버리면
   다시시작할수가 없으니 어떻게좀해봐"). 출정·여정 종료·보급·갈림길은 콜백이 정확히 한 번
   와야 앞으로 가는 화면이라, 콜백 없이 사라지면 **아무도 안 누른 채 영원히 기다린다.**
   닫히지 않게 막는 대신 **닫혀도 되게** 고쳤다: 고르기를 기다리는데 화면이 없으면
   다음 프레임에 다시 연다(`game/loop.ts reopen`). probe-boot 6절이 이 회귀를 잡는다.

### 2026-08-31 추가 12 — 산전·연주전: 한 번 당겨 세 발 (형의 요구)

형: **"샷건같은 3개 동시쏘는 화살도 필요할거고 한번만 쏴도 빠르게 조준방향대로 3발
연사하는것도 있어야하고."**

둘 다 "한 번 당겨 세 발"인데 **벌어지는 축이 정반대**라 성격이 갈린다 —
산전은 **공간**(부채꼴)으로, 연주전은 **시간**(연속)으로 벌어진다.

1. **산전(散箭)** — 한 번에 셋이 부채꼴로 나간다 (반각 0.10rad ≈ ±5.7°, 10m에서 좌우 1m).
   값은 **사거리**로 치른다: 굵고 가벼운 몸(mass 0.5 · caliber 1.7)이라 공기저항이 커서
   가까이서는 셋 다 꽂히고 멀면 셋 다 못 간다. 산탄총이 실제로 그런 물건이라
   물리를 그대로 쓰면 성격이 저절로 나온다 — 숫자를 손으로 박지 않았다.

2. **연주전(連珠箭)** — 첫 발 뒤로 0.11초 간격에 둘이 더, 방향은 **첫 발 그대로**.
   가만한 것에는 셋이 겹쳐 꽂히고(단일 표적에 강하다), 바람·이동 과녁에는 흩어진다.
   예약은 활(bow)이 아니라 **월드**가 든다 — 당김이 끝난 뒤에 나가는 살이라 활에 걸어
   두면 다음 당김이 지운다. 시각은 타이머가 아니라 `elapsed`로 잰다 (A1).

3. **재고는 한 발만 깎인다.** 셋을 깎으면 두 살은 '연출이 화려한 보통 살'이 되고 고를
   이유가 사라진다. 곁가지·뒷발은 splitDepth 1이라 miss도 안 뱉는다 — 분열 자식과 같은
   규칙이다(당김은 한 번이다). 그래서 명중률 분모가 오염되지 않는다.

4. **판 끝 판정에 예약을 센다.** `arrowsLeft 0 && rapidLeft 0` — 안 세면 마지막 한 발을
   발치에 꽂았을 때 그 프레임에 실패가 확정되고 뒷발은 영영 안 나간다. 한 번 당겨 셋을
   산 값을 못 받는 자리였다 (`tests/volley.test.ts` 가 이 회귀를 잡는다).

5. **소리도 둘로 갈렸다** (발사음 9색). 산전은 8ms 간격으로 거의 겹치는 **넓은** 쉭 셋 +
   살대끼리의 덜그럭. 연주전은 첫 발이 얇은 쉭 하나뿐이고, 뒷발은 별도의 `rapid` 사건이
   점점 여려지는 시위 소리로 낸다 — 셋으로 들려야 하는 건 실제로 셋이 나갈 때다.

6. **보급 풀** — 산전은 2마디(애기살과 같은 칸: 겨누기가 서툴 때 "붙으면 된다"는 두 번째 답),
   연주전은 3마디(육량전과 같은 칸: 한 방이 크다 vs 셋을 나눠 때린다).

밸런스: 9종 × 62판 교차표에서 산전 3/62 · 연주전 5/62 점수 1등, 지배 전략 없음 ✓.

### 2026-08-31 추가 11 — 격차 분석 · 화약 상자 · 시체 · 가로 겹침 (형의 반려 8건)

이날 형이 연달아 여덟을 짚었다. **docs/GAP.md (격차 분석)가 이 묶음의 머리글이다** —
"이미 있는 좋은 사례들의 게임모델과 흐름, 성장, 킬링포인트 가져오라고. 우리게임과 적절하게."

1. **가로 화면에서도 버튼이 궁수를 덮던 것** (형: "아직도 가로화면에서 (…) 난 이해가 안된다").
   세로만 고쳤던 게 맞다. 가로는 여전히 위아래 여백 0.12로 가운데 정렬이었는데, DOM 버튼은
   언제나 화면 **왼쪽 아래**에 있고 궁수도 거기 선다. 이제 **화면형 셋**(세로 폰 190px /
   눕힌 폰 120px / 데스크탑 170px)이 각자 아래 띠를 비우고, 가로는 그 **빈 자리의 가운데**에
   구도를 잡는다. 눕힌 폰은 버튼을 한 줄로 고정(nowrap)해 띠가 자라지 않는다.
   프로브가 다섯 해상도에서 **지면이 바 윗선 위인지**를 잰다.

2. **화약 상자(barrel) — 과녁이 아닌 물건** (형: "다이너마이트상자 (…) 그건 과녁으로 안치고").
   `TargetKind 'barrel'` 을 새로 만들었다: 클리어 조건에 안 들어가고, 점수를 안 주고,
   좌상단 숫자에도 안 세고, 맞으면 무조건 터진다. 그림도 링이 아니라 **나무 궤짝 + 도화선**이다.
   `convertToFoes` 가 상자를 건너뛰므로 11판+ 에서도 살아남는다 — 이게 핵심이었다.
   그전까지는 저작된 모든 것이 예외 없이 적으로 바뀌어서 **폭발이 놓일 자리가 판에 없었다.**

3. **폭약을 쓰는 판을 만들었다** (형: "이걸 활용한 맵을 만들어야 퍼즐을 풀만할거아냐").
   저작 판 다섯(9·16·19·37·46판)을 상자 퍼즐로 다시 짰다 — 상자를 중심으로 과녁이
   폭발 반경 안 고리에 서고 화살은 정답 발수 + 여유만 준다. 좌표는 생성기가 냈고
   겹침·지면 관통·반경 밖을 전부 수치로 배제했다. 갈림길 '화약고'도 같은 규칙으로
   **상자를 직접 놓고 화살을 조이는** 카드가 됐다 (예전엔 아무 데나 표시만 하고 화살은
   그대로 줘서 그냥 쉬워지는 카드였다 — 그게 "딱히 재미는 없는데"의 정체다).

4. **좌상단이 적과 과녁을 나눠 센다** (형: "너라면 과녁이 사람이랑 같겠냐?").
   `적 3 · 과녁 2` 처럼 있는 것만 말한다. 상자는 어느 쪽에도 안 센다.

5. **쓰러진 적이 남는다** (형: "퍽하고 뜨거나 날아가거나 물리엔진 적용해서").
   `foe_down` 이벤트에 **그 죽음을 만든 충격**(속도·질량)이 실려 나가고, 시체는
   렌더가 굴린다 — 중력·지면 반발·마찰·회전, 누웠다가 6초 뒤 사라진다.
   초속 = 화살 속도 × 질량비 × 전달률이라 **무거운 살이 몸을 더 밀어낸다.**
   사람은 접힌 몸으로, 드론은 부러진 십자로 남는다. sim에는 상태를 안 만든다 (A1).

6. **폰 숨참기 버튼** (형: "폰에서는 숨참기 못하니까"). HUD 줄 맨 왼쪽, 손가락 화면에만.
   그전까지 폰 사용자는 떨림을 죽이는 손이 **아예 없었다** — 조작 하나가 통째로 빠져 있었다.

7. **지도 점프가 실제로 된다** (형: "지도에서 해당 스테이지로 가는게 나는 안된다").
   안 됐던 게 맞다. `save.runActive` 면 그냥 돌아섰는데 여정은 늘 진행 중이라
   **볼 만한 상황에서는 언제나 막혀 있었다.** 이제 두 번 눌러 확인하고 그 자리에서 연다.

8. **패널 테두리 위로 글자가 지나가던 것** (형: "종이위에서 글자가 왔다갔다 하는거같잖아").
   문양 테두리 두께만큼 안쪽 칸을 물렸다 — 글자가 테두리에 닿기 전에 잘린다.

9. **소리를 늘렸다** (형: "sfx가 많아야해"). 패배음 · 스탯 올림 · 쓰러지는 소리(사람=퍽,
   드론=금속). 예전 규칙이던 "실패에는 소리를 얹지 않는다"를 형이 뒤집었다 —
   다만 조롱은 여전히 안 한다: 클리어음의 절반도 안 되는 크기로 한 번만.
   패배음은 임시다(인터페이스 부저) — 한 소절짜리 곡으로 바꾸는 건 형이 후보를 듣고 고를 일이다.

### 2026-08-31 추가 10 — 화전이 진짜 터진다 · 갈림길 재작 · 아이콘 · 모바일 바 (형의 반려 4건)

형: **"아이콘이랑 이미지 리소스들 최대한 더 받아오라고. 올릴 스탯들 근력이면 근력, 정확도
등등의 아이콘(일반적인 RPG게임에서 보이는 제대로된 이미지여야함). 그리고 바람골 밀집
고르는거 너무 별로야. 진짜 개노잼이야. 심지어 밀집으로 하면 과녁끼리 어이없이 겹치거나
땅바닥에 쳐박혀서 맞힐수도 없는게 나오잖아. 그리고 몇스테이지 가면 버튼이 캐릭터를
가린다고 모바일 세로로 플레이할때. (…) 그리고 화전은 어딜 맞춰도 폭발해야해. 심지어
땅에맞아도 터져야한다고. 내발앞에 떨어지면 나도 데미지 맞아야지."**

1. **화전(火箭)이 어디서 끝나든 터진다.** 예전 폭발은 `resolveHit` 안에만 있었다 —
   **과녁을 맞혀야만** 터졌다. 빗나간 화전은 그냥 흙에 꽂힌 막대기였다.
   - `sim/target.ts` 의 burst를 좌표 기준 `burstAt(w, x, y, R, exclude)` 로 열고,
     `sim/ballistics.ts` 가 **화살이 죽는 자리**(착지·수명 초과)에서 부른다.
   - **자해**: 폭발 반경의 62%(`P.arrowkind.burstSelfRange`) 안에 궁수가 서 있으면
     적 화살과 같은 크기(25)로 내 체력이 깎인다. 궁수를 점이 아니라 **서 있는 몸**
     (발 y=0 ~ 활 손)으로 재는 게 핵심이었다 — 활 손 한 점으로 재면 발밑 폭발이
     1.4m 밖으로 계산돼 안 아팠다.
   - 테스트 5건 (`tests/fire.test.ts`).

2. **갈림길을 통째로 다시 짰다** (`game/forks.ts`).
   - **원인 진단**: "개노잼"의 정체는 선택지가 둘인 게 아니라 **50판 내내 같은 둘**이었다.
     그건 결정이 아니라 확인 버튼이다.
   - 이제 **여섯 장의 패**에서 매판 둘: 화공(이 판의 살이 전부 화전·재고 안 씀) ·
     화약고(과녁 절반이 폭탄) · 바람골 · 노다지(보급 과녁 하나) · 척후(돌진 하나) ·
     단발(화살을 적게, 훈련치 크게). 축이 전부 다르고, **직전 판과 같은 짝은 안 나온다**
     (loop가 직전에 보여준 짝을 다음 뽑기에 넘긴다).
   - **밀집은 버렸다.** 있는 과녁을 무작위 방향으로 복제하는 방식은 "겹치면 다시 뽑기"를
     붙여도 지면 관통·건물 밖 창문 사수·복제끼리 겹침을 구조적으로 못 막는다. 이제 새로
     놓는 것(노다지·척후)은 **기존 과녁의 오른쪽 바깥**에만 서고, 나머지 넷은 과녁을
     아예 새로 놓지 않는다. 60판 × 두 카드에 대해 겹침·지면 관통 0을 테스트가 지킨다.
   - 테스트 22건 (`tests/forks.test.ts`).

3. **아이콘을 받아 왔다** — game-icons.net (CC BY 3.0) 14개, `public/icons/`.
   - 스탯 넷이 제 얼굴을 얻었다: 근력=이두, 정확=과녁, 지구력=폐(호흡), 집중=좌선.
   - 길잡이 버튼 넷(성장·수집·지도·실험장)과 갈림길 카드 여섯도 전부 아이콘을 단다.
   - 쓰는 법: `background: currentColor` + `mask-image` — **색을 CSS가 입힌다.**
     그래서 잠긴 줄은 아이콘까지 흐려지고 강조된 줄은 아이콘까지 강조된다.
     검은 배경 사각형 한 줄만 걷어냈다 (`tools/strip-icon-bg.mjs`). 출처는 `public/icons/출처.txt`.

4. **모바일 아래 버튼 바** (형: "몇 스테이지 가면 버튼이 캐릭터를 가린다").
   - 원인은 크기가 아니라 **줄 수**였다: 글자가 붙은 버튼이 넉 줄까지 접히며 위로 자랐고,
     살통에 살 종류가 늘수록 더 자랐다 — 그래서 "몇 스테이지 가면" 나빠졌다.
   - 폰에서는 글자를 지우고 **아이콘만** 남긴다. 46px 칸이 최대 두 줄(길잡이 + 살통),
     살통은 넘치면 옆으로 민다. 높이가 고정되므로 카메라가 그만큼(190px) 자리를 비운다.
   - `ui/overlay.ts` 의 버튼 높이와 `render/camera.ts` 의 `bandBottomPx` 는 **같이
     움직여야 하는 짝**이라 프로브가 둘을 맞대어 본다 (probe-style ⑦).
     실제 투영도 잰다: 390x844·360x640에서 **지면이 바 윗선보다 위**인지 (probe-ui).

### 2026-08-31 추가 9 — 세로 화면 · 검정 걷어내기 · 문양 테두리 (형의 반려 3건)

형: **"모바일에서 세로로 잘 작동하게 UI 싹 추가정리해줘. 그리고 너무 새까매 전체적으로.
버튼이나 화면에 뜨는 박스, 팝업 이런것들은 좀 스타일있게 다운받은 리소스 써야 하는거 아니냐?"**

셋 다 맞는 말이었다.

1. **세로 화면 (폰).** 두 층을 같이 고쳤다.
   - **구도** (`render/camera.ts`): 세로에서는 좌우 여백을 0.12 → 0.045로 줄이고,
     가운데 정렬 대신 **지면을 아래 띠(0.2h)에 붙인다.** 폭에 묶여 남던 세로 공간이
     전부 하늘이 되고, 그 위에 HUD가 얹힌다. 390x844에서 1판 지면 y=656/844.
   - **패널** (`ui/overlay.ts`): 가운데 상자 → **아래에서 올라오는 시트**(90dvh, 손잡이,
     safe-area). **닫기 ✕ 를 만들었다** — 예전엔 Esc와 바깥 클릭뿐이라 폰에서는 닫는
     방법이 사실상 없었다. 누르는 것은 전부 44px 이상, 키 힌트(Tab·C)는 터치 기기에서 숨긴다.
   - 좁은 화면용 접기: 판별 별 격자 10칸→5칸 · 성장 줄의 버튼을 아래로 · 지도판은
     칸 폭에 맞춰 통째로 축소(--map-s) · 실험장 막대를 캔버스 HUD 아래로.
   - 자막·결과 배너도 세로에서는 아래로 내렸다 — 360x640에서 화살 눈금을 밟는 걸
     프로브가 잡았다.

2. **검정을 걷어냈다.** 감마 리프트(0.70~0.92)로 **관계를 유지한 채** 통째로 올렸다.
   - 하늘 다섯 시각(`render/sky.ts`) · THEME(`render/camera.ts`) · 도시(`render/buildings.ts`).
     1장 하늘 `#0b0e13/#1a222c` → `#1c2129/#343e4b`. 능선 3겹의 간격은 오히려 넓어졌다
     (감마는 어두운 쪽에서 차이를 벌린다).
   - 실루엣 계약은 그대로다 — 몸 대비 7.8:1 (하한 5:1, `npm run probe` 하늘 프로브).
   - 패널은 순검정(`#0f151d`)에서 **따뜻한 먹지**(`#2f2e29~#262521`)로. 글자도 같이 올려
     가장 어두운 글자(--mute)도 8.0:1 이다.

3. **테두리·글꼴을 받아 왔다** (기성 애셋 우선).
   - **Kenney 'Fantasy UI Borders'** (CC0)의 井자 창살 문양을 border-image 9-slice로.
     한 장 **173바이트**다. 흰 마스크의 **팔레트 항목을 직접 금색으로 바꿔** 넣었다
     (`tools/tint-frames.mjs`) — CSS filter는 브라우저마다 다르고 mask-border는
     파이어폭스가 못 읽는다. 패널에 하나, 카드(활·보급·갈림길)에 하나.
   - **글꼴**: 제목·큰 글자는 명조(Gowun Batang 700), 숫자는 Barlow Condensed.
     자체 호스팅(public/fonts) · unicode-range 조각 · font-display: swap이라
     첫 페인트를 막지 않는다. 캔버스는 웹폰트를 스스로 안 받아오므로
     `overlay.warmFont()` 로 판 이름·결과 한마디를 한 번 미리 요청한다.
   - 종이결만은 받지 않고 SVG 잡음으로 만들었다 (수백 바이트 · 배율 자유).

4. **`tools/probe-style.ts` 를 새로 만들었다** (프로브 9번째).
   probe-ui.ts 는 캔버스만 재고 "DOM 오버레이는 형이 눈으로" 였다 — 그래서 **폰에서
   패널을 못 닫는다**는 사실이 몇 달 동안 아무 게이트에도 안 걸렸다. 이제 CSS 문법 ·
   토큰 정의 · 글자 대비 7:1 · 애셋 존재 · 세로 규약(✕·safe-area·44px·dvh)을 숫자로 본다.
   probe-ui.ts 에도 세로 두 해상도(390x844 · 360x640)와 **화면 밖 검사**를 넣었다.

5. `tools/budget.ts` 가 public/ 사본을 실제 파일 목록으로 대조해 제외한다.
   예전엔 확장자만 봐서 `public/fonts/fonts.css` 가 '번들 CSS'로 잡혔다.

### 2026-08-26 추가 7 — 적 목 붙이기 · 칭호 장착 · 갈림길 2택 (형: "밋밋하고 재미없다")

형이 셋을 한 번에 지적했다: 적 궁수 머리가 몸에서 떨어져 보임 · 칭호가 얻어도 못 고르고
재미없음 · 게임이 전반적으로 밋밋함("메가히트1인개발자 전문가 렌즈로 대격변 업데이트").

1. **적 목이 없었다** (`render/foe.ts`). 척추는 어깨→골반으로만 그려졌고 머리(원)는
   어깨에서 0.32×rx만큼 떨어진 자리에 아무 연결선 없이 떠 있었다 — 스틱맨(플레이어)은
   어깨→목→머리가 이어져 있는데 적만 빠져 있던 것. 목 선 하나 추가로 끝.
2. **칭호 장착** (세이브 v10). 예전엔 "가장 어려운 칭호"가 자동으로 고정 표시였다 — 고를
   수 없었다. 수집 화면에서 딴 칭호를 클릭해 장착(`save.equippedTitle`). 8개 칭호 이름도
   전부 국궁 말을 살린 칭호로 개명 (예: 백발→**백중(百中)의 손**, 흠 없는 활→**완궁(完弓)**).
3. **갈림길 2택** (MEGAHIT.md §3, 여러 세션째 미뤄오던 마지막 큰 항목). 판이 끝나면
   바람골/밀집 중 하나를 고른다. 바람골=바람 강화+훈련치 배수, 밀집=과녁 복제(+화살도
   같이 지급)+클리어 시 특수살 재고 +1. `game/forks.ts`(순수 함수, 결정론) ·
   `SPEC.fork`(라이브 튜닝) · 테스트 15건.
   - **balance-lens가 첫 실측값(훈련치×1.6)을 지배 전략으로 잡아냈다** — 챕터1에서
     바람을 5배(30m/s)까지 올려도 클리어율이 안 흔들려 "위험 없는 확정 +60%"였다.
     ×1.15로 내려 밀집(같은 9판 기준 176.5 훈련치)과 맞췄다 (`docs/BALANCE.md` §7).
   - **첫 구현(캔버스 힌트 문자열 + 1/2 키)을 형이 그 자리에서 반려했다**: "그따위로
     텍스트로 선택하게 하는 게임이 어딨냐?" — 보급 3택과 같은 카드 모달로 다시 짰다
     (`ui/loadout.ts mountFork`). 아래 추가 8 참조.

### 2026-08-26 추가 8 — 갈림길을 카드로 · 헤드샷 숫자 거짓말 · 폭탄 과녁

같은 날 형이 플레이해보고 셋을 더 짚었다.

1. **갈림길을 진짜 카드로** (형: "텍스트로 선택하게 하는 게임이 어딨냐"). 캔버스 힌트
   문자열+숫자키였던 첫 구현을 버리고 보급 3택과 똑같은 카드 모달로 바꿨다
   (`mountFork`, `ui/loadout.ts`). 판마다(보스 제외) 뜨고 클릭 한 번이 곧 다음 판 —
   건너뛰기 개념 자체가 사라졌다(진짜 선택이니까). 입력단의 1/2 키 핸들러(`input/pointer.ts`
   takeFork)는 죽은 코드가 되어 걷어냈다.
2. **헤드샷 숫자가 몸통샷보다 작아 보이던 것** (형: "몸에 맞았는데 144뜨고 머리맞았는데
   70뜨는건뭐냐"). 원인: 적 궁수 헤드샷은 체력 무관 즉사라, 뜨는 숫자가 "넣은 피해"가
   아니라 "그때 남아 있던 체력"이었다 — 이미 몸통샷으로 깎인 채 죽으면 작게 뜬다.
   `SimEvent.hit.execute` 플래그를 추가해 즉사 헤드샷은 숫자 대신 **"즉사!"**를 띄운다
   (보스 헤드샷은 원래도 고정값 200이라 이 문제가 없었다 — 안 건드림).
3. **폭탄 과녁** (형: "맞히는 오브젝트중에 폭발하는 폭탄이라던가 이런것도 넣어야지").
   `TargetSpec.bomb` — 어떤 화살로 죽여도(살 종류 무관) 죽는 순간 `P.target.bombRadius`
   안의 과녁을 같이 친다. 폭발 살과 같은 함수(`target.ts burst()`)를 재사용해 반경을
   `max(화살의 burstRadius, 과녁이 방금 죽었고 bomb라면 bombRadius)`로 일반화했다 —
   그래서 "살아남으면 안 터진다"(보스·궁수가 버티면 아직)와 "재귀 안 한다"(연쇄로
   죽은 폭탄은 또 안 터진다, burst() 자체가 원래 갖고 있던 규칙)가 공짜로 따라왔다.
   아직 캠페인·무한 구간에는 안 심었다 — **실험장(샌드박스)의 '폭탄' 버튼으로만** 있다.
   `npm run dev` → 실험장 → 폭탄을 형이 먼저 보고, 반경·시각 효과(halo 점선 링)가
   괜찮으면 그 다음에 실제 판에 배치할 자리(어느 챕터·어느 테마)를 정한다.
   테스트 4건(`tests/bomb.test.ts`) — 반경 안/밖, 안 죽으면 안 터짐, 재귀 금지.

**형이 눈으로 봐야 하는 것**: 갈림길 카드가 보급 3택과 결이 맞는가 · 즉사 팝업 "즉사!"가
헤드샷!과 겹쳐 보이지 않는가 · 폭탄의 점선 halo(2.2Hz 맥박)가 거슬리지 않고 잘 읽히는가.
   - **더 급한 별도 발견**: `tools/balance-sim.ts`가 11판+ 실제 전환(`convertToFoes`,
     적 궁수 반격)을 안 쓰고 원저작 과녁으로 재고 있다 — `docs/BALANCE.md`의 챕터2~4
     기존 수치 전부가 영향권. **다음에 손댈 사람에게 넘김**, 이번엔 안 고쳤다.

**남은 것 (MEGAHIT §8)**: 살의 곱셈(판 중 화살 교체) — 유일하게 남은 구조 변경 항목.
**형이 눈으로 봐야 하는 것**: UI/UX가 "무슨 웹페이지 같다"는 지적 — 아직 손 안 댐
(형이 "갈림길 먼저"로 순서를 정함). 다음 세션 후보 1번.

> **2026-08-25~26은 메가히트 패스였다.** `docs/MEGAHIT.md`가 그날의 설계 문서이고,
> 아래 '추가 6'이 무엇을 만들었는지의 목록이다. 이어서 작업하려면 MEGAHIT §8(렌즈)의
> 우선순위 표와 §11(살의 물리)을 먼저 읽어라.

> 번들 — 전송 gzip **68.1KB / 90KB** · 파싱 raw **186.1KB / 260KB** (C6, `npm run budget`).
> 2026-08-25 형의 판단으로 자를 고쳤다: raw 150KB 하나 → gzip·raw 둘. 여유가 넉넉하다.

### 2026-08-25 추가 4 — 활 그립 · 건물 · 적 궁수 · 스테이지 머리글 (형의 반려 4건)

형이 한 번에 넷을 반려했다. 넷 다 "그림이 사실과 다르다"는 같은 종류의 지적이었다.

1. **활을 쥔 손** (형: "손이 활을 안 잡고 붕 떠 있는데다, 당기면 활 잡은 위치가 오히려
   손보다 앞으로 나가버려"). 범인은 stickman.ts의 `apex` — 활 호의 정점을 활손보다
   과녁 쪽으로 밀되 그 양이 당김에 비례해 커졌다(0.5→1.6배). 정점 오프셋을 없애고
   **그립 = 활손 좌표**로 못박았다. 당김이 옮기는 건 시위가 걸린 두 팁뿐이다.
   라이저(손잡이) 구간 + 양손 주먹을 추가 — 활대를 쥐고 줄을 쥔 게 눈에 박힌다.
   당김 0의 노크도 시위 위로 내렸다 (예전엔 시위가 활보다 앞으로 볼록했다).
2. **건물** (형: "1·2·3층으로 쌓으려면 제대로 쌓고" / "적군이 죽었는데 건물은 왜 없어져").
   뿌리가 하나였다 — **벽을 적이 들고 있었다.** `render/buildings.ts`로 분리해서
   판 시작에 한 번 굽고(bake), 누가 죽든 건물은 남는다. 층·열 규칙: 사수의 y가 층의
   기준선이고 벌어진 사이는 같은 간격의 빈 층으로 채운다. 맨 아래 층에서 땅까지도
   같은 간격. 가까이 선 사수들은 한 건물을 공유한다 (벽이 포개지지 않는다).
3. **적 궁수** (형: "활 똑바로 좀 잡고 쏘게 만들고 적군도 좀 제대로 만들어라").
   `render/foe.ts` 신설. 예전 적의 활은 몸 옆(-x)에 못박혀 내가 어디 있든 왼쪽을
   겨눴고, 손은 활에 안 닿았고, 화살은 아예 없었다. 이제 조준축이 **나를 향하고**,
   그립은 활손에 고정, 당기면 팁이 젖혀지고 시위손이 턱으로 오며, 예고 동안
   **화살이 시위에 물려 있다.** 플레이어 스틱맨과 같은 문법이다.
4. **좌상단 스테이지 머리글** (형: "매우 마음에 안 드니까 메가히트들이 어떻게 하는지
   보고 배워서 적용해"). 예전엔 통산 번호('63판') + 이름 + 흩어진 점 열 개였다.
   메가히트의 문법 셋만 가져왔다 — ① 마리오 'WORLD 1-1': 길잡이는 통산 번호가 아니라
   **장-판 쌍**이다(26px 주인공) ② 셀레스트: 장의 **이름**이 쌍 옆에 조용히
   ③ 배틀패스·뱀서: 흩어진 점이 아니라 **하나의 트랙**, 마지막 칸은 마름모(보스).
   통산 번호는 트랙 끝의 각주(11px)로 내렸다.

프로브 둘 신설 — `probe-form.ts`(그립 흔들림 0.00px · 활손↔활대 0.00px · 팁 젖혀짐
7.8→23.7px 단조) · `probe-city.ts`(층 간격 균일 · 벽 겹침 0 · **전멸 후 건물 동일**).

### 2026-08-25~26 추가 6 — 메가히트 패스 (형: "C레벨 임원이 들어왔다고 생각하고")

형의 요구로 게임 전체를 다시 봤다. 설계 문서는 **`docs/MEGAHIT.md`** 이고 여기는 목록만 둔다.

**진단 (§0)** — 밸런스 시뮬에 **판 소요 시간 축을 새로 만들어** 재보니 처음 쓴 진단이 틀렸다.
숙련자는 이미 3.2배 빨리 끝낸다 (nov 13.75s / avg 7.17s / exp 4.25s · 시간 격차 68%).
문제는 실력에 **효과는 있는데 표현도 보상도 없다**는 것이었다.

1. **번들 예산의 자를 고쳤다** (형: "크기 굳이 꾸역꾸역 작게 제한할 필요 없어").
   raw 150KB 하나 → **전송 gzip 90KB · 파싱 raw 260KB** 둘. GDD C6은 "CI에서 잰다"고
   써놓고 실제로는 안 재고 있었다 — `tools/budget.ts`가 이제 진짜로 막는다.
2. **연사와 몰기** (`sim/flow.ts`) — 맞히면 다음 만작이 빨라진다(×0.88 누적, 하한 0.55).
   5중(한 순)이면 **몰기**. 판이 아니라 **여정**을 넘어 이어진다 (캠페인 판은 과녁이
   1~4개라 판 안에서는 몰기에 구조적으로 도달할 수 없었다). 정확도·탄도는 한 톨도 안 건드린다.
   실측 기여: 몰기에서 판 소요 −14%. **연사는 속도의 원천이 아니라 증폭기다.**
3. **자기 최고 시간** (세이브 v8) — 4.25s와 13.75s가 같은 별을 받고 있었다.
   시간은 이 게임에서 **천장이 없는 유일한 축**이다. 결과 배너에 `12.4초 최고 9.8초`.
4. **활터의 북** — 형이 '관중'에서 콜로세움을 떠올린 그 자리다. 인정의 원형이 구경꾼인데
   밤 실루엣 위에 군중을 얹을 수는 없다(C1). 국궁엔 이미 답이 있었다 — 맞히면 북이 운다.
   픽셀 0 · 조준 안 가림 · M으로 꺼진다. 한 순 눈금 5칸 + 몰기의 금빛 궤적도 같이.
5. **여정 종료를 발사대로** (세이브 v9) — "가져간다 훈련치 N · 별 N · 몰기".
6. **하늘이 장이다** (`render/sky.ts`) — 여명 전 → 새벽 → 안개 아침 → 노을 → 밤.
   '한낮'은 **뺐다**: 하늘을 밝히면 실루엣 계약이 깨져 전 색을 뒤집어야 한다.
   그림자·궤적 taper·촉 앞의 공기도 같이. `probe-sky`가 장마다 몸 대비 ≥5:1을 잰다 (실측 7.1~12.9).
7. **'한 발' 순간** — 마지막 하나를 향해 내 화살이 나는 동안만 시간이 0.4배로 늘어진다.
   히트스톱과 같은 급이라 결정론은 무사하다.
8. **살의 물리를 질량 하나에서 파생** (§11, 형: "데미지는 화살 속도랑 무게에 따라도").
   살마다 손으로 박던 speedMul·dragMul·dmgMul·pierceExtra·armorPierce를 없애고
   **질량과 단면적 둘만** 저작한다. 초속 `1/√(m+Mv)` · 항력 `A/m` · 피해 `m·v²` ·
   관통력 `(m/A)·v`. 가상질량이 없으면 총구 에너지가 모든 살에서 같아진다(실측 64 vs 65).
   교차표: homing 27→16(지배 깨짐) · pierce 0→11(죽은 카드 부활).
   갑옷 몸통 처치: 보통 살 0/4 · 애기살 3/4(5발) · 육량전 3/4(3발).
9. **점수 팝 → 피해 팝** (형: "화살이 맞으면 점수가 아니라 데미지가 떠야").
   체력 없는 과녁에는 아무 숫자도 안 띄운다. 갑옷에 튕기면 '막혔다'.
   8번의 물리가 화면에 보이는 유일한 창이 이 숫자다.
10. 곁다리 수리 — **활 그립**(당길수록 활이 손에서 도망가던 것) · **건물**(적이 죽으면
    같이 사라지던 것) · **적 궁수**(활이 몸 옆에 못박혀 있던 것) · **스테이지 머리글**
    (흩어진 점 열 개 → 장-판 쌍 + 트랙) · **몸 전체가 두꺼워지던 것**(이제 몸통만 1.17배).

**남은 것 (당시 기준)**: 갈림길 2택(판마다 결정 하나 — MEGAHIT §3, **2026-08-26 추가 7에서
완료**). 육량전은 갑옷을 열지만
점수 교차표에선 1/62다 — 그 표가 점수만 재기 때문이고, 값은 `0/4 vs 3/4`에 있다.
형이 "점수로도 쓸모가 있어야 한다"고 보면 질량이나 heavyScoreMul을 손대야 하는데
후자는 예전에 지배 전략을 만든 노브라 신중해야 한다.

**형이 눈·귀로 판정해야 하는 것**: 북 음량(`P.audio.drumGain 0.2`) · '한 발'의 늘어짐
(`P.render.oneShotScale 0.4`) · 노을 장(4장)의 색감 · 피해 숫자가 화면을 덮지 않는가.

### 2026-08-25 추가 5 — 발사음 재작 (형: "활 쏘는 게 병아리 소리도 아니고")

원인이 둘이었고 둘 다 소리의 **설계** 문제였다.

1. **몸통이 없었다.** 릴리즈가 삼각파 420→160Hz 하나였다. 진짜 활 소리를 지배하는 건
   시위가 아니라 **림이 브레이스로 되돌아가며 치는 저역 텅**(80~200Hz)인데 그게 통째로
   빠져 있었으니 가슴에 오는 게 없고 중역 삑만 남았다.
2. **쉭이 거꾸로 올라갔다.** swoosh 1500 → **3200**Hz. 멀어지는 화살은 음이 내려간다.
   올라가는 노이즈 스윕은 물리적으로 '다가오는 것'이라 활 소리가 될 수 없다.

그래서 릴리즈를 실제 활의 세 겹으로 다시 세웠다 — **텅**(림의 저역: 사인 210→78 +
저역 노이즈) · **탁**(시위 크랙: 밴드패스 1750→900, Q 3.4, 35ms) ·
**쉭**(밴드패스 5200→**1100**, Q 6, 200ms — 내려간다).

살별 목소리도 음정(오실레이터)에서 **바람(필터 노이즈)** 으로 옮겼다.
- **애기살=편전** (형이 직접 짚은 것): 사각파 2300→3100Hz 30ms 삑이었다. 편전은 통아에
  얹어 쏘는 반 길이 살이라 초속이 두 배 가까우니, 서명은 '더 높고 좁고 빠른 쉭'이다.
  통아 나무 틱 + 7200→1800Hz Q9 쉭 + 3000→1150Hz Q12 꼬리.
- 육량전: 저역 노이즈 몸통 + 느린 저역 쉭(1700→480). 편전과 정반대 축.
- 세전: 삼각파 3연타 → 어긋난 쉭 셋(4200/3600/3000Hz).
- 화전: 심지 파직임 아래에 불길이 끌리는 후욱(760→300)을 깔았다.
- 신전: 저역 받침 추가. 명적은 **손대지 않았다** (형: "설정 자체가 그러니까").

`probe-audio.ts`에 발사음 판정 추가 — ① 300Hz 아래 몸통이 있는가 ② 노이즈 스윕이
**내려가는가** ③ 2kHz 위 60ms 삑(=병아리)이 없는가. 7종 전부 통과, 편전 7200Hz >
기본살 5637Hz. 붐빔(편전+명중+연쇄) 동시 14개 < 상한 18.

곁다리: `probe-render.ts`의 깃발 검사가 **거짓말을 하고 있었다** — 천 색이 #c99a5e로
바뀐 걸 모르고 옛 #c96f4a로 찾아 "바람 깃발 없음 ⚠"을 4건 띄웠다. 깃발은 멀쩡했다.
고치니 2·4·6 m/s에서 -44°→-16°→0°로 제대로 눕는다.

**형이 귀로 판정해야 하는 것**: 텅·탁·쉭의 비율(P.audio의 releaseGain / releaseBodyGain /
releaseSnapGain / swooshGain 넷이 그 노브다), 편전의 쉭이 충분히 날카로운가.

**형이 눈으로 봐야 하는 것**: 창밖으로 내민 활의 각도, 건물의 층수 느낌, 머리글의 크기감.

### 2026-08-25 추가 3 — 맞불 · 줌아웃 비율 · 게이지 여운 (오늘 마무리)
1. **화살 맞불** (형): 내 화살이 적 화살을 공중에서 쳐낸다 (반경 P.enemy.deflectR 0.3m,
   양방향 점-선분). 적 화살만 죽는다. '쳐냈다!' 팝 + 불꽃 + 쇠 챙. tests/deflect.test.ts.
2. **줌아웃 스틱맨 비율** (형: "두꺼워지고 대두"): 픽셀 하한(minPx 3.6·머리·시위)이
   기준 배율 26 아래에서 배율에 비례해 줄어든다 (stickman.ts LINE.refScale).
3. **게이지 여운** (형): 스태미나 바·내 체력 바는 당길 때만 뜨고, 놓으면 2초 뒤 0.4초
   fadeout. 체력 바는 체력이 변한 순간에도 뜬다. 시계는 sim elapsed (탭 숨김과 결이 같다).
4. 오마케 → **샌드박스** 개명 (라벨·식별자·파일명 전부).

**다음에 손대면 좋은 것 (미결):**
- 밸런스 시뮬 봇이 숨는 사수(look 2)의 은신을 모른다 — 21판+ 클리어율이 실제보다 낮게 나온다.
- 봇이 적 궁수의 사격/우선순위도 아직 모른다 (8/24부터 이월).
- 적 머리 위 체력 바는 상시 표시다 — 형이 원하면 여운 규칙으로 통일.
- 감사 P2 잔여 항목: docs/AUDIT-2026-08-25.md (베테랑 체크포인트 출발, .hb-sec 색 토큰 통일 등).
- 방패병(헤드샷 무효·틈새 노리기) 적 변종 후보.

### 2026-08-25 추가 2 — 곡사 챕터 5 · 창문 사수 재작 · 드론 스프라이트
1. **챕터 5 '곡사'** (41~50판, 캠페인 40→50판): 과녁이 55~112m. 위로 겨눠 포물선을
   그려야만 닿는다 (실측 만작 사거리 320m, 필요 앙각 2.5~7.8도 — 실제 국궁 편사 각).
   챕터 5도 11판+ 규칙으로 전환되므로 원거리 저격수 판이 된다. 적 화살 초속 26→36
   (이게 없으면 먼 창의 적 화살이 나까지 못 왔다).
2. **창문 사수 재작** (형의 반려): 벽은 땅에서 자라고(허공 창문 금지), 창턱 위
   상반신만 보이고(클리핑 — 하반신은 벽 뒤), 숨은 사수의 창은 빈 창이다.
   활만 창밖으로 나온다. 갑옷병은 투구 머리.
3. **드론 = 기성 스프라이트** (형: "가져올 수 있는 건 가져와 써"): OpenGameArt CC0
   'Pixel Art Drone' 2프레임. public/sprites/ + src/render/sprites.ts (없으면 벡터 폴백 —
   헤드리스 프로브는 Image가 없어 항상 폴백을 검사한다). 출처는 public/sprites/출처.txt.
   건물 팩은 3D(Kenney building-kit)거나 밝은 카툰 톤이라 밤 실루엣과 충돌 — 건물은 벡터 유지.

### 2026-08-25 추가 — 11판+ 전면 적 전환 · 특수살 발사음 · 샌드박스
1. **11판부터 과녁이 전부 적이 된다** (형): static·pierceable→창문의 사수(look 1),
   moving→숨었다 쏘는 사수(look 2, 예고 직전에만 나온다), aerial→드론(look 3).
   `convertToFoes`가 저작 판 배치를 그대로 쓰고 껍데기만 바꾼다. 무리 크기만큼
   개별 발사 주기(firePeriod)를 벌려 화살비를 막는다.
2. 보급이 종류를 그림으로 말한다 — 화살(픽토그램)/기력(초록 십자). 16판+ 일부 보급은 기력.
3. 특수살 6종 발사 목소리 (형: "쏘는 소리가 각각 달라야") — 명적=우는살 호루라기,
   화전=심지 파직임, 애기살=채찍 스냅, 육량전=낮은 텅, 세전=3연 파닥임, 신전=웅웅.
4. 폭주귀신 재작 (형: "성의없다") — 잔상 2벌 + 화염 갈기 3가닥 + 찢어진 아가리.
5. Arrow.lastHit — 큰 적 몸속 재충돌이 관통 예산·사슬 도약을 삼키던 버그 수리.
   명적 사거리 4.5→7.5m. 샌드박스 실험장(소환·판 점프)도 이날 함께.
- 테스트 75건. **밸런스 시뮬 봇은 숨는 사수를 모른다(은신 중 사격 낭비) — 다음 일.**

### 2026-08-24 추가 4 — 적과 체력 (RUN.md 6장)

- 체력 3, 여정 동안 지속, 0 = "쓰러졌다" 여정 종료. HUD 하트.
- 11판부터 적 궁수(예고 후 발사·조준선 텔레그래프), 돌진=접촉 피해, 보스=접촉 즉사.
- 릴리즈 팔로스루(시위만 튕기고 손은 반동 후 복귀) · 통아 덜렁거림 · 과녁 띠 불투명.
- 번들: 튜닝 라벨을 빌드에서 벗겨(146.3KB) 예산 복귀 — 배포판 콘솔은 노브 경로 표시.
- 테스트 66건. **밸런스 시뮬의 적 반영(봇 피격 대응)은 아직 안 봄 — 다음 일.**

### 2026-08-24 추가 3 — 살통(특수살 재고) · 활 실루엣 재작도 · 조준 원뿔

1. **살통**: 특수살은 해금이 아니라 **재고**다 (RUN.md 4장). 유엽전만 무한, 장전=판당
   1발(HUD 버튼, 다음 판 적용), 획득=보스 보급 3택(마디 깊이가 풀을 정한다).
   세이브 v6 — 옛 화살 해금은 종류당 2발 재고로 환산.
2. **활 실루엣**: 실물 원리로 재작도 — 림은 휘고 고자는 안 휜다(각궁 반곡의 정체).
   애기살=편전: 통아(화살 길이 홈통)가 시위 손에 남고 반 길이 살만 난다.
3. **조준 원뿔**: 커서가 궁수 뒤로 가면 릭이 접혀 "작아졌다 커졌다" 하던 버그.
   조준각을 앞쪽 원뿔에 가둠.
4. 클리어음 후보 15종 + 듣기.html (`sound-candidates/`) — 형이 직접 고른다.

### 2026-08-24 추가 2 — 여정(로그라이트) 전환 · 보스 · 결정론 완성

형의 연속 요청으로 게임의 뼈대가 바뀌었다. **docs/RUN.md 필독.**

1. **여정 구조**: 판마다 3택 폐지 → 런 시작에 활+살통 선택. 화살이 바닥나면 여정 종료,
   도달 판·점수 기록(bestRun). R = 여정 포기(2회 확인). 영구 성장은 유지(로그라이트).
2. **보스**: 10판마다 (id는 그 챕터의 `-10`). 체력·머리 약점(치명타=정중앙 축 재사용),
   닿으면 그 판 패배. 관통 불가. 마디마다 배치 변주 + 호위.
3. **숨은 산포 제거**: releaseScatter 0 — 오차는 화면에 보이는 떨림뿐.
   FOCUS는 호흡의 스탯으로 재편(깊이+한계 시간).
4. **탭 사거리**: minSpeed 18→6 (탭 39m → 11m).
5. 클리어음 상승형 교체(실측 -4.4반음이었다) · 글자 대비 7:1 · 활 외형(스틱맨+걸이) ·
   활 밸런스 시뮬 검증 통과 · 번들 terser로 139.7KB.

**남은 일 (v2)**: 보스 처치 시 강화 픽 3택 · 오프라인 화살→런 시작 보너스 재해석 ·
밸런스 시뮬의 여정 모드(런 단위 도달 판 분포) · 보스 연출(등장 자막·처치 이펙트) 강화.

### 2026-08-24 추가 — 활 시스템 (형의 두 요청)

1. **화살 실명 개명**: 유엽전·화전·명적·세전·신전·애기살·육량전. 효과를 이름에 맞췄다
   (애기살=관통은 역사 그대로). 드래프트 카드에 한자·유래 자막.
2. **돌진·보급 과녁** + 무한 테마 12종. 돌진은 화살 1발만 빼앗는다 (체력·게임오버 없음, C2).
3. **활 5자루** — docs/BOWS.md. 메가히트 1인겜 조사에서 셋을 가져왔다:
   활=규칙(발라트로 조커) · 궁합=진화(VS: 각궁×애기살=편전, 장궁×육량전) ·
   숙련=무기성장(케이브 스토리, 단 벌 없음). 성장 화면(Tab)의 활 걸이에서 장착,
   해금 4칸(수집 화면), 숙련은 그 활로 맞힌 수가 대가를 깎는다.
   sim은 BowMods(순수 배수)만 안다 — 결정론 유지, 테스트 8건 추가.

**형이 눈으로 볼 것**: 활 걸이 UI(Tab), 드래프트 카드의 궁합 줄, 활별 손맛 차이
(각궁 빠른 당김 / 컴파운드 렛오프). **활 밸런스는 아직 시뮬 미검증** — balance-sim에
활 축 추가가 다음 일이다.

```
npm run dev      →  http://localhost:5173
npm test         →  49건
npm run balance  →  봇 자동 플레이 (--endless=N 으로 무한 구간 표본)
npm run probe    →  부팅 / 화살 / 소리 / 화면 프로브 4종
```

조작: 좌클릭 홀드→놓기 · `Shift` 호흡정지 · `Tab` 성장 · `C` 수집 · `M` 음소거 · `R` 재시작 ·
`` ` `` 튜닝 콘솔 · 드래프트에서 `1`/`2`/`3` 또는 `Esc`(건너뛰기)

---

## 1. 이번에 고친 것 (형의 반려 목록)

형이 준 목록과 그에 대한 답이다.

| 반려 | 한 일 |
|---|---|
| **갈래 화살이 손에서 나간다** | 원인 확정 후 수정 + 회귀 테스트. 아래 2장 |
| **일정 스테이지 뒤엔 똑같은 과녁만 나온다** | `getStage`가 40판으로 자르고 있었다. 41판부터 생성기 도입 |
| **성공음·또로롱이 밋밋하다** | 배음 합성 voice(`bellTone`) 추가, 클리어·연쇄·해금 전부 다시 만듦 |
| **UI가 AI가 만든 티 나고 작다** | 캔버스 HUD·DOM 패널 전부 다시. 크기 대폭 확대 |
| **그래픽·재미요소가 부족하다** | 과녁 조형, 바람 깃발, 판 머리글·자막, 박힌 화살, 하늘·풀 |

### 아직 형이 눈으로 봐야 하는 것
숫자로는 답이 안 나오는 것들이다. 프로브로 좌표·크기·예약 시각까지는 확인했다.

- [ ] 소리가 실제로 좋게 들리는가 (클리어 상승음 · 연쇄 또로롱)
- [ ] 새 과녁 조형이 어두운 배경에서 잘 보이는가
- [ ] 판 시작 자막이 거슬리지 않는가 (1.6초, 아무것도 막지 않는다)
- [ ] UI 크기가 이제 충분한가 (1280 기준 화살 수 46px · 본문 15px)
- [ ] 양궁 자세가 자연스러운가 (`docs/FORM.md` 규격은 좌표로 통과)

---

## 2. 갈래 화살 버그 — 원인과 수정

`src/sim/ballistics.ts`

명중으로 죽은 부모 화살은 그 순간 **풀에서 가장 앞선 빈 슬롯**이 된다.
자식 배정(`freeSlot`)이 부모를 배제하지 않아서 첫 자식이 부모 슬롯에 앉았고,
`launch()`가 그 슬롯의 `pendX/pendY`(태어날 자리)를 0으로 지웠다.
그래서 **두 번째 자식부터 월드 원점(0, 0) = 궁수 발치**에서 태어났다.

수정은 둘이다 (하나만으로도 되지만 빗장은 둘이어야 한다):
1. `freeSlot(w, except)` — 부모 슬롯을 배정 대상에서 뺀다.
2. `spawnSplit`이 부모의 값을 **루프 전에 복사**한다.

회귀 테스트 `tests/arrows.test.ts` — 예전 코드에서 발사점 `(0.000, 0.000)` 재현 확인.

부수 효과: 부모의 시체가 살아남아 유령 궤적(`captureTrail`)도 제대로 잡힌다.
`npm run probe` 의 화살 비교표에서 분열 살이 6개 중 3개 제거로 올라왔다.

---

## 3. 무한 구간 (41판~)

`src/game/endless.ts` · 공용 킷 `src/game/stagekit.ts`

- 테마 10종: 사열 · 기둥 · 진자 · 관통줄 · 아치 · 군집 · 계단 · 바람골 · 고리 · 폭풍
- **결정론.** 배치 난수는 `seedFrom('hanbal.endless.layout.' + 판번호)` 하나에서 나온다.
  sim의 `w.rng`(릴리즈 산포)와 완전히 분리돼 있다 (A1).
- **한 바퀴(10판)에 모든 테마가 정확히 한 번씩.** 블록 셔플 + 경계 보정이라
  같은 테마가 연달아 나오는 게 구조적으로 불가능하다. 테스트로 고정했다.
- 각크기는 `stagekit.angularSize()` 가 h=0.012 → 0.008 로 40판에 걸쳐 내리고 거기서 멈춘다.
  **크기로 어렵게 만들지 않는다** — 형이 한 번 반려한 방향이다.
- 화살은 "직접 맞혀야 하는 수(shots) + 2". 연쇄 판에서 화살이 남아돌지 않게.

밸런스 시뮬로 벽 세 곳을 찾아 고쳤다 (`npm run balance -- --endless=30`):

| 판 | 증상 | 원인 | 수정 |
|---|---|---|---|
| 41+5 | 클리어 0% | 기둥 3열 = 과녁 12개인데 화살 상한 10 | 열을 2로 고정, shots를 `과녁수 - 열수`로 |
| 41+16 | 클리어 5% | 같은 원인 | 위와 같음 |
| 41+17 | 클리어 20~34% | 이동 과녁 4개 + 주기 0.43Hz | 3개로 고정, 주기 상한 ↓, 크기 +30% |

지금은 표본 30판 전부 66% 이상. (대부분 봇 기준으로는 쉬운 편인데,
"앞 구간은 쉽게"라는 정책과 봇이 사람의 체감을 못 재는 한계를 감안한 의도적 선택이다.)

---

## 4. 소리

`src/audio/synth.ts` 의 `bellTone()` — **배음이 여럿이고, 위 배음이 먼저 죽고, 정수배가 아니다.**
이 셋이 "울리는 소리"의 전부다. 오실레이터 하나로는 아무리 성형해도 "삑"이지 "또로롱"이 아니다.

- **판 클리어**: 저음(C3) + 종 4음(C5·E5·G5·**C6**) + 음정 없는 반짝임. 총 1279ms.
  옥타브로 닫는 게 중요하다 — 5도로 끝내면 "아직 안 끝났다"로 들린다.
- **연쇄**: 유리 샘플 위에 종의 배음을 얹는다. 기본음 300 → 450Hz 반음씩 상승 (실측).
- **해금**: 딸깍(샘플) 위에 두 음. 딸깍만으로는 방향이 없어 그냥 버튼 소리였다.
- 버그 하나 같이 고쳤다: `echoScale` 의 `lastAt` 이 0으로 열려 있어
  **각 종류의 첫 소리가 통째로 버려지고 있었다** (AudioContext는 currentTime 0 근처에서 태어난다).

`npm run probe` 가 예약 시각·음정·게인을 표로 찍는다 (`tools/probe-audio.ts`).

---

## 5. 화면

### 월드 (`src/render/scene.ts`)
- **과녁**: 얇은 원 두 개 → 면으로 채운 띠 넷 (밝은 테 / 어두운 띠 / 강조 / 흰 중심).
  어두운 배경에서 작은 과녁이 묻히던 문제와 "중심이 어디인가" 둘 다 해결. 색은 안 늘렸다.
- **종류가 모양으로 읽힌다**: 관통 = 마름모, 이동 = 왕복 레일, 낮은 과녁 = 지면 기둥,
  공중 = 후광 + 매단 줄.
- ★ **바람 깃발.** 26판부터 바람이 부는데 화면에 바람이 **하나도 없었다**.
  GDD가 적어둔 "깃발을 보고 기다렸다 쏜다"가 성립할 수가 없었다.
  풍속 2 m/s에서 -44°, 4에서 -16°, 5 이상 수평 (렌더 프로브 실측).
- 화살에 촉과 깃 / 하늘에 달·별 / 지면에 풀 / **빗나간 화살이 땅에 박혀 남는다**
  (다섯 발이 과녁 아래 나란히 박혀 있으면 조준이 아니라 낙차를 못 읽고 있다는 게 보인다).
- 한 프레임 draw 호출 125회.

### HUD (`src/render/hud.ts`)
- ★ **판 번호와 판 이름이 지금까지 화면 어디에도 없었다.** 머리글 + 판 시작 자막(1.6초).
- 전부 창 크기에 비례. 1280×800 기준 화살 수 30 → **46px**, 과녁 16 → 23px. 1920에서 62px.
- 글꼴에 역할을 줬다 — 한글 스택 / DIN 계열 숫자 스택.
- 바람 방향·세기 눈금 (오른쪽 위).

### DOM 패널 (`src/ui/`)
- 본문 13 → 15px, 패널 520 → 680px, 드래프트 카드 132 → 190px.
- 모서리 6px 라운드 → 2~3px. 강조는 **한 곳에만** — 패널 위 3px 띠, hover의 왼쪽 잉크 바.
- 디자인 토큰(`--ink`, `--line` …)과 공통 클래스(`.hb-sec`, `.hb-lead`)를 overlay가 한 곳에서 정의.

---

## 6. 현재 수치

| | |
|---|---|
| 코드 | TS 약 12,000줄 |
| 테스트 | 49건 전부 통과 |
| 번들 | **48KB gzip** (예산 60KB) + `public/sfx` 221KB(번들 외, 지연 로딩) |
| 난이도 | 캠페인 40판에 LOW 없음 · 무한 표본 30판 전부 66%+ |
| 화살 균형 | ✓ 지배 전략 없음 |

---

## 7. ★ 다음에 할 일

### 1순위 — 아직 남은 밸런스 숙제
- [ ] **★★★이 44%** (목표 5~30%). 앞 40판이 쉬워서 무손실이 기본값이 됐다.
      별을 조이는 게 아니라 난이도 정책과 같이 봐야 한다.
- [ ] **보상의 실력 반영이 약함** (효과크기 0.26, 목표 ≥1.0).
      점수→훈련치 **환산율**을 키워야 한다 = 성장 곡선 전체 재측정.

### 2순위 — 아직 안 만든 것
- [ ] 활 업그레이드 (지금은 화살만 있다)
- [ ] 무한 구간 전용 보상 (지금은 캠페인과 같은 규칙을 쓴다)
- [ ] 배포 (아래 8장)

---

## 8. 배포 — 형이 한 줄 쳐줘야 한다

원격 저장소가 아직 없다. 로컬 커밋만 쌓여 있다.

```
! gh auth login
```

이걸 프롬프트에 치면 그다음은 전부 자동으로 된다:
레포 생성 → 푸시 → GitHub Actions로 Pages 배포 (`.claude/skills/ship/SKILL.md` 참조).
**공개(public)로 할지 비공개(private)로 할지 물어볼 것.**

---

## 9. 이어서 작업할 때 주의할 것

- **브라우저 도구를 쓰지 마라.** 연결된 크롬은 전부 다른 기기다 — 이 PC의 localhost는 안 열린다.
  `CLAUDE.md` 최상단에 규칙이 있다. 화면 확인은 **프로브 4종**으로 하거나 형에게 요청한다.
  - `tools/probe-boot.ts` 부팅·저장·마이그레이션
  - `tools/probe-arrows.ts` 화살 7종 단발 비교
  - `tools/probe-audio.ts` 소리를 표로 (예약 시각·음정·게인)
  - `tools/probe-render.ts` 화면을 표로 (글자 크기·자리·깃발 각도·draw 호출 수)
- **"쏘는 대로 맞는다"(빨간 바 계약)를 되돌리지 마라.** 난이도는 배치와 메커닉으로 만든다.
- **앞 40판을 어렵게 만들지 마라.** 형이 "수십 단계까지는 쉽게"라고 명시했다.
- **기성 에셋을 먼저 찾아라.** 처음부터 만들지 않는다 (형의 지시).
- 정관 훅이 저장을 차단하면 논쟁하지 말고 지적대로 고친다.
  (특히 `src/sim`·`src/game` 의 A2 매직넘버 검사 — 이름을 붙여 상수로 올리면 통과한다.)
